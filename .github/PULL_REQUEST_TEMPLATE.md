## What this changes

<!-- One or two sentences. What can the user do now that they could not before,
     or what stopped being broken? -->

## Why

<!-- Link the issue, or describe the problem. -->

## Checklist

- [ ] `python -m pytest` is green
- [ ] `npm --prefix frontend run build` succeeds (if the frontend changed)
- [ ] New or changed behaviour is covered by a test
- [ ] `README.md` / `docs/` updated if behaviour changed
- [ ] No personal data: machine paths, names, emails or tokens
- [ ] `config.json`, `data/` and `frontend/dist/` are not in the diff
