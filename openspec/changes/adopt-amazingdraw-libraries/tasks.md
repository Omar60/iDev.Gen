## 1. The guard

- [x] 1.1 Write every refusal marker and every fixture in escape sequences so no tracked file carries a non-English glyph, and verify a test asserts each escaped marker still matches the text it was written for
- [x] 1.2 Add the deny-list and the guard function in backend code, covering the four refusal signals (source library, identifier, tags, theme text) and the minor-coded profile keys, and verify a unit test refuses one fixture entry per signal
- [x] 1.3 Make the guard un-overridable: no flag, env var, config key or caller argument may include a refused entry, and verify a test asserts that passing any such argument raises rather than imports
- [x] 1.4 Add the reporting path so a refused entry is counted and named by identifier but never has its text written or logged, and verify a test captures the report and asserts no theme text appears in it
- [x] 1.5 Add invented fixture entries under `tests/` so the guard's tests run with the external libraries absent, and verify the guard suite passes on a checkout with no source directory present - satisfied by construction across 1.1 to 1.4: every fixture is invented and no guard test reads a directory. What shipped for it is the assertion that keeps it true, on the guard suite's own import list
- [ ] 1.6 moved to 4.3, below. Written here it has nothing to watch: no import path exists until phase 4, so the test would iterate an empty registry and pass forever

## 2. The translation pass

- [x] 2.1 Define the translation map keyed by source string, at an untracked path beside the source material, with the source string, the English translation and the fields it covers, and verify a test rejects a map entry whose translation is empty or still contains non-English characters
- [x] 2.2 Write the extractor that lists every non-English string in the accepted entries of a source directory given as a required argument, running the guard first so refused entries are never listed, and verify a test asserts a refused fixture's strings are absent from its output
- [x] 2.3 Translate the highest-value tier first — the `notes` and the four `*_anchor` fields on accepted entries — and verify every translated string is present in the map and no source string in that tier is left uncovered
- [x] 2.4 Translate the room labels for every accepted entry, and verify the map covers each accepted room's label with no gaps
- [x] 2.5 Translate the remaining accepted fields — shot variants, non-English pose hints, and the profile display names, descriptions and identity names — and verify the extractor reports zero uncovered strings across the accepted corpus. Closed at 4 strings, not the 363 the field list implies, because 4.2 landed first: the profile libraries stopped being accepted corpus, and with them 256 celebrity names and 104 amateurs rows. What was left of the accepted corpus was three shot variants and one library description. There were no uncovered pose hints at any point. Measured: 0 uncovered across 511 rows, 511 against an empty map as the control
- [ ] 2.6 moved to 4.3c, below. Written here it has nothing to watch: nothing in the app reaches source prose yet - `main.py` imports neither the extractor, nor the translation map, nor the guard, and the three `/api/*/import` routes that do exist read this project's own measured catalogue. The import this constrains is built in 4.3, so the test would run against nothing and pass forever. Same reason 1.6 moved. The live translator today is `backend/enhance.py`, whose system prompt tells the model to answer in English whatever language it was asked in. That is the prompt assistant translating the operator's own request, which is what it is for. The string this task protects cannot reach it until an imported room's text can fill a look
- [ ] 2.7 moved to 4.3d, below. Written here it has nothing to watch: nothing in the app reaches source prose yet - `main.py` imports neither the extractor, nor the translation map, nor the guard, and the three `/api/*/import` routes that do exist read this project's own measured catalogue. The import this constrains is built in 4.3, so the test would run against nothing and pass forever. Same reason 1.6 moved. Its own criterion says it: a test that imports a string appearing in two libraries
- [ ] 2.8 moved to 4.3e, below. Written here it has nothing to watch: nothing in the app reaches source prose yet - `main.py` imports neither the extractor, nor the translation map, nor the guard, and the three `/api/*/import` routes that do exist read this project's own measured catalogue. The import this constrains is built in 4.3, so the test would run against nothing and pass forever. Same reason 1.6 moved. Its own criterion says it: a test that runs the import twice
- [x] 2.9 Add the repository-wide test that no tracked file contains CJK - the Han, Hiragana, Katakana and Hangul ranges - and verify it fails against a deliberately planted fixture before it passes. Operator's decision, taken after measuring: 119 of 210 tracked files already carry non-ASCII, all of it typographic punctuation this project wrote on purpose (2052 em dashes, curly quotes, arrows) plus UI emoji in `frontend/src/kinds.js`, and zero CJK. Written as "non-ASCII" the test is born failing against half the repo and gets "fixed" with an allow-list of 119 paths, which is a test that cannot fail with extra steps. The property that matters is the one the guard spec is about: no source prose in its own script. Shipped as `CJK_PATTERN` in `tests/test_no_personal_data.py`, beside the personal-data and image rules, over the same tracked-and-not-yet-staged file list so a new file fails before it is committed. It fired on two things the day it was written: four ComfyUI workflow JSONs carrying one node title exported from a Chinese distribution, now on `ALLOWED_CJK` by name because rewriting a saved graph's titles is not free; and `scripts/shoot_amazingdraw_full.py`, where four camera-family names sat in comments that already carried their own English gloss, so the gloss replaced them
- [x] 2.10 Add the test that no tracked file carries imported source prose, and verify it fails against a fixture room text planted in a tracked seed before it passes. Written as the rule that makes the planting impossible rather than a scan for prose nobody can recognise yet: everything under `data/` is ignored, and a file reaches the repo only through a `!` line somebody wrote. Git keeps tracking a file added before its ignore rule, so the two lists had already drifted - `directed-acts-seed.json`, `directed-act-readings-seed.json` and `wardrobe-seed.json` were tracked with nothing naming them, and `.gitignore` now says what is true. Proved by removing one `!` line and watching it fire
- [x] 2.11 Add the test that no string from a refused entry appears in the translation map, and verify it names the entry when it fails. Fixture-based, because the map is untracked and absent from a fresh checkout: a refused room and a not-adopted profile, each with its string planted in the map, and the report names `school_corridor_01:label` while carrying neither the string nor its translation - a report that quoted the prose to prove the prose is there would publish it to say so. Measured over the real map the same day: 0 of 497 keys come from a refused entry

## 3. The registry

- [x] 3.1 Define the room registry shape in config (library name, seed file, enabled, weight) with a documented default, and verify the app starts with the default and lists the existing nine rooms
- [x] 3.2 Replace the hard-coded room seed filename with a registry read, and verify the existing room picker still shows the same nine rooms - moved to 6.4. The only hard-coded room seed filename in the app is the build-time import at `frontend/src/views/ModelDetail.jsx:13`, which 6.1 requires stays exactly where it is, and no runtime path carries the registry to the picker until 6.4 serves one. What phase 3 landed is the registry the read will come from; the read itself has nowhere to live yet CLOSED BY 6.4 AND 6.7. The runtime read is `GET /api/rooms` over the registry, joined to the build-time import by `pickerRooms`; the hard-coded filename stays exactly where 6.1 requires it, as the tracked half the bundle carries. The nine are still the nine: `test_app_starts_with_no_registry_key_and_lists_nine_rooms` reads them through the registry, and 6.7's vitest counts the shipped ten under each manner - nine under candid and selfie, ten under directed.
- [x] 3.3 Honour the enabled flag so a disabled library contributes no rooms to the picker or a draw while remaining readable, and verify a test toggles a fixture library and asserts both directions - half done, and the half that ships is the half nobody sees. The loader honours the flag and a test toggles a fixture library in both directions, but the picker is still the build-time import and no draw exists until 6.15, so setting `enabled: false` today removes a library's rooms from a return value nothing reads. Closes with 3.2 in 6.4 CLOSED BY 6.4 AND 6.15. Both consumers exist now and neither sees a switched-off library: the picker is fed by `/api/rooms`, which contributes an empty list and the sentence "switched off in the room library registry" (asserted in `tests/test_room_import_api.py`), and 6.15's draw is over that same list. The loader half was already there and still is - `enabled_only=False` reads a disabled library whole, which is what an operator inspecting one needs.
- [x] 3.4 Add the test that the registry and the seed files on disk agree in both directions, and verify it fails for a registry entry naming a missing file and for a seed file no entry names
- [x] 3.5 Update README.md and the matching page under `docs/` for the registry setting, and verify the documented default matches the shipped one

## 4. The refresh pipeline

- [x] 4.1 Declare each source library with the kind of material it carries and every destination its entries reach, and verify a test asserts an undeclared file is refused and writes nothing. A destination is a file this app reads at runtime, and exactly one kind exists for imported material: a room seed the registry names. The `component` table is not declared, because nothing writes mined rows to it and a declaration listing tables nothing writes to cannot be wrong. The six room libraries name one seed each; `amateurs` and `celebrities` name none, on the guard's own `SIGNAL_NOT_ADOPTED` imported rather than respelled; and `perspective_scenes` names none on `not_split_yet`, because its entries fuse a camera clause into room prose and a room that carries a camera position overrules the line's camera. The library comes from the FILE NAME and nothing inside the file. The six destination filenames do not exist yet - 4.3 and 5.12 create them - so what the declaration is checked against is the registry's own `is_room_seed_file` plus uniqueness across libraries, which is falsifiable today. Written by a subagent, reviewed by probe: every refusal called directly, the whole tree compared byte for byte across a refusal, and the undeclared rule broken to watch three tests fail before restoring it. A destination is a file this app reads at runtime, and exactly one kind exists for imported material: a room seed the registry names. The `component` table was NOT declared - nothing writes mined rows to it, and a declaration listing tables nothing writes to is a declaration that cannot be wrong. The six room libraries name one seed each, checked against the registry's own `is_room_seed_file` and required unique across libraries, so a typo or a duplicate fails. A library with nowhere to write declares no destination and is refused on its own reason, which gives three distinct refusals with no per-kind branching: `undeclared`, `not_adopted`, `not_split_yet`. `perspective_scenes` is the third - it fuses a camera clause into room prose, and a room row carrying a camera position is a room that overrules the line's camera. The library is read from the FILE NAME and nothing inside the file. The six destination filenames do not exist yet; 4.3 and 5.12 create them
- [x] 4.2 Declare the body-profile source as material this project does not adopt, and verify a test asserts it is refused with that reason and that the reason is distinct from a deny-list refusal. Taken out of order, ahead of 4.1, because 2.5 could not be answered without it: one reading translates four strings and the other translates 363 including 256 real people's names. It needed none of 4.1's manifest - `NOT_ADOPTED_LIBRARIES` is its own list with its own signal, checked after the deny-list so a library on both reports the licence refusal. `amateurs` and `celebrities` are on it
- [x] 4.3a Add the test that fails when an import path writes seed rows without consulting the guard, and verify it fails against a deliberately unguarded stub before it passes - this is 1.6, landed here because 4.3 builds the first import path for it to name. A test of this name already existed and was already falsifiable - removing the guard filter outright fails it - so what was short was its reach, and the shortfall is measured rather than reasoned: every refused fixture the suite fed the import path was refused on `identifier`, and a probe raising inside `import_source` on a refusal carried by any OTHER signal never fired across the whole suite, so the case was not merely unasserted, it was unreached. An import path that consulted the guard and wrote the row anyway on `theme_text`, `tags` or `minor_profile_key` passed all 592 tests. Shipped as one refused fixture per signal that can reach an entry INSIDE a declared library, keyed by that signal and asserted to be refused on it - without that first assertion the test goes quietly vacuous the way it already had, four fixtures refused on one signal reading as four signals covered. The assertion itself is behavioural and reads rows back off the written seed file, shared by the real path and the stub as one function, because a source scan for `guard_entries` is exactly what the mutation below defeats: a path that imports the guard, calls it and writes the row regardless reads identically to a scan. The control arm is `_unguarded_import_stub`, which writes every entry as a row and which the same function names all four refusals in. Measured by one break, restored, and re-run with this test deselected at 591 passed: leaking every entry refused on a signal other than `identifier` back into the merge. The whole-guard break (`accepted_entries = list(all_entries)`) is NOT this test's - two other tests already catch it, and with this one deselected the suite still goes red at 589. Deliberately not covered: `library` and `not_adopted`, which 4.1 refuses at the file before an entry is ever loaded and which `test_manifest_and_guard_refusals_run_before_writing` already covers; a second import path, because a grep over `backend/` and `scripts/` finds exactly one function that writes seed rows - `import_source`, one `write_text` at `backend/importer.py:432` - and no test can reach code that does not exist; and the CLI entry, because `test_app_operation_and_cli_entry_produce_identical_seed_content` already proves the app's and the CLI's seed content byte-identical, so a row absent from one is absent from the other. Unaffected by the two known-and-left defects: the assertion matches on `identifier` and never on the key `derive_room_key` collapses, and no fixture carries `offers`
- [x] 4.3 Implement the import once - refusal, translation lookup, merge, report - and verify a test asserts the app operation and the command-line entry produce identical seed content for one fixture source. Measured before writing any of it, against a temporary data dir: `room_registry.verify_registry_disk_agreement` has no safe order. Writing the seed first fails direction 2 (`Room seed file on disk is not named in registry`); adding the registry entry first fails direction 1 (`Registry entry names missing seed file`); and `enabled: false` does not exempt a library from needing its file on disk. So writing the seed and registering the library are one operation from outside, or the check states that the in-flight pair is allowed. Nothing calls that check in the app today - `main.py` imports `room_registry` nowhere, and its only two callers are tests using `tmp_path` and a fixture config - so an import that gets this wrong breaks nothing until 6.4 serves the registry over a route. Which is the reason to settle it here rather than after Closed. One implementation, `backend/importer.py:import_source`, called directly by the app and by `scripts/import_assets.py`, which is the whole of the CLI - so the criterion's byte-identical seed content is structural rather than kept in step by hand, and `test_app_operation_and_cli_entry_produce_identical_seed_content` is what states it. The order question the note above measured is answered by refusing to have an order: the seed write and the registry append happen inside the same loop over destinations, `config` is a required keyword with no default, and `verify_registry_disk_agreement` runs once at the end - so a caller that could omit the registry half is a caller that cannot reach the write. `test_seed_and_registry_are_written_as_one_operation` holds it. Ticked at review after re-running the gates rather than on report: 602 tests, 0 failures. The pair can still come apart on ONE path, and it is not this one - a refusal raised AFTER the seeds are on disk, which only exists once something in the app calls this. That is 4.9's, found there by probe and fixed there
- [x] 4.3c Make the map the only source of translations: the importer reads it and never translates during a run, and verify a test asserts an import stops and names the entry and field when a string is missing from the map - this is 2.6, landed here because 4.3 builds the first import path for it to constrain. NOT satisfied by construction, which is what the measurement was for: the stop and the translation are two separate recursions over one entry, and they did not agree. `_find_untranslated_strings` walks every top-level key through the extractor's `_extract_strings_from_value`, which descends a dict, a list of strings and a dict inside a list - and not a list inside a list, the branch 4.5 reverted for being unreached. `_translate_value` descends all of them, and `notes` (site 357) is the one structured value handed to it, the other two sites (337 label, 343 theme) being `str` by construction. So a `notes` field shaped `[[text]]` was reachable by the writer and invisible to the check, and its string branch ended `return val` - the source's own script handed back unchanged. Measured end to end on that fixture before anything was changed: the import did not stop, `written == 1`, and the seed carried the source script as the \uXXXX escapes `ensure_ascii=True` writes back out and the repository's CJK rule cannot see. That is 4.4's defect in a second place. Fixed on the writer, not on the check: `_translate_value` now raises `TranslationMissingError` for a non-English string the map does not carry, and carries `identifier` and a dotted `field` so the refusal names both - `notes.ambience`, the same path the coverage walk names. Fixing the check instead would close the one shape found and leave two hand-kept recursions to drift again; the writer is where all three sites route through, and a fourth site added in phase 5 inherits the refusal rather than the hole. Presence is the whole check because `validate_translation_map` already refuses an empty or still-non-English translation on the way in (2.1). Still before any write: rows merge in memory and the seeds are written after the loop, asserted against a pre-existing destination's bytes rather than an empty directory. Measured by three breaks, each restored from a byte copy. (1) Restoring the old `return val` fallback fails only the new test - 593 passed with it deselected, which is the 592 baseline plus the translator test. (2) The mutation this task names - the coverage walk skipping `notes`, a field the translation walk still writes - is NOT this test's: with the fix in place it can no longer put prose on disk, only shorten the uncovered list, and 4.5's `test_translation_refusal_lists_every_uncovered_string_and_excludes_refused_entries` catches it at 591 passed with both new tests deselected. Said plainly rather than chased: the fix moved that failure mode from silent to already-covered. (3) A function-level `import backend.enhance` inside `import_source` fails only `test_no_translator_is_reachable_from_an_import_run` - 593 passed with it deselected. That second test is the run-time half of the criterion, and it is a subprocess on purpose: `backend/enhance.py` is loaded under the top-level name `enhance` and `httpx` is in `sys.modules` outright by the time the suite reaches this file, so an in-process check is either vacuous or red for unrelated reasons. A fresh interpreter runs a real import and reports which of `backend.enhance`/`enhance`/`httpx` it loaded; the answer is none, and at import time `backend.importer` pulls in only `backend.asset_guard`, `backend.extractor`, `backend.room_registry`, `backend.source_manifest` and `backend.translation_map`. Deliberately not covered: the uncovered-string LIST degrades to one item for a shape the coverage walk misses, since the writer raises on the first one it meets - the full list stays 4.5's job and stays complete for every shape the coverage walk does reach. `offers` is still copied to the row without the map (4.5's open note, left as found), and it is not a hole: the coverage walk reaches it like every other key, so a non-English `offers` stops the run rather than landing - measured on str, list-of-str and dict shapes. No row field is untouched by both walks; `identifier` and `source_library` are written raw and both are walked, `key` is ASCII-normalised by `derive_room_key`, and `manner` is a literal. Unaffected by the `gs_01`/`gs-01` key collapse: the assertion is on the refusal's `identifier` and `field`, and the passing half reads a row back by `notes`, never by the collapsed key. `backend/extractor.py` is untouched - the list-inside-a-list branch stays out of it, as 4.5 left it Followed up the same day: the writer's refusal is the backstop, not the reporter. `_extract_strings_from_value` now descends a list inside a list, so the coverage walk names every uncovered string in these shapes and the full list survives - see the correction on 4.5. The writer still refuses, and stays the thing that makes a future gap between the two walks fail loudly instead of silently
- [x] 4.3d Assert one source string yields one English string everywhere, and verify a test imports a string that appears in two libraries and asserts both rows carry the same translation - this is 2.7, landed here because 4.3 builds the first import path for it to constrain. Satisfied by construction - `_translate_value` is a pure lookup over one map loaded once per run, and nothing keys it by library - so what shipped is the assertion that keeps it true: one shared source string imported from two libraries into two destination seeds, `general_scenes` and `workplace_scenes`. Shared on TWO fields, not one: asserting only on the label would pass a divergence on any other translated field, and the theme reaches the row as `look` through a second `_translate_value` call. Proved by two separate breaks, each restored - a per-library suffix on the label, and an upper-cased theme on one library only
- [x] 4.3e Assert re-running an import rewords nothing, and verify a test runs the import twice and compares every stored translation - this is 2.8, landed here because 4.3 builds the first import path for it to constrain Ticked after operator review of the two corrections. The fixture is ONE config handed to both runs, which is the production shape and the only one that executes the registry's `already registered, do not append again` branch - with two fresh configs `existing_seed_files` is empty on run 2 and a duplicate entry goes unnoticed. Verified here, not taken on report: `if dest not in existing_seed_files:` forced to `if True:` fails this test and no other (591 passed with it deselected), and so does replacing the `unchanged`/`updated` split with an unconditional `updated_keys.append(room_key)`. The label-rewording mutation is NOT this test's - it belongs to `test_merge_updates_text_and_preserves_verdict_and_orphaned_rows`. Scope accepted deliberately: `label`, `look` and `notes` are compared because they are the three sites that call `_translate_value` (`backend/importer.py` 337, 343, 357), so `every stored translation` is complete for today's row shape and not for tomorrow's - `offers` is copied without the map, noted and left in 4.5, and a fourth `_translate_value` site added in phase 5 would leave this test short without failing
- [x] 4.4 Run the refusal rule and the translation lookup over the whole upload before writing anything, and verify a test asserts a source with one uncovered string leaves every destination byte-identical. Written by a subagent, reviewed by probe, and the probe found what reading the diff did not. The walk was filtered by the extractor's `METADATA_FIELDS`, which is the list a coverage report ignores and not the list of what may reach a seed file: `identifier` is on it AND is written to the row, so a non-English identifier stopped being refused and landed on disk as unicode escapes `ensure_ascii=True` writes back out and the repository's CJK rule cannot see. Measured both directions on one fixture - HEAD refuses it, the change wrote it - which is the whole reason the filter is gone and every field is walked now. The check runs BEFORE any write and AFTER the empty-header skip: an entry with no theme text is a file-level header leftover, not a refused entry and not a written one, so a string on it cannot reach a seed and must not take the upload down with it
- [x] 4.5 List every uncovered string with the field it came from when an upload is refused for translation, and verify a test asserts a refused entry's strings are absent from that list. The list is collected over every accepted entry rather than raised on the first, deduplicated by identifier, field and string, and `TranslationMissingError` carries it as `uncovered` while keeping `identifier`, `field` and `text` pointing at the first item. Absent means absent for three reasons now: a guard signal, an unadopted library, and an empty header. Nested fields are walked by reusing the extractor's own recursive walker, so a path like `notes.ambience` is named rather than escaping to disk. Two pieces of the subagent's delivery were reverted rather than kept: a constructor accepting the old three-argument form, which had zero callers in `backend/` and zero in `tests/`, and an extractor branch for a list inside a list, which left the whole suite green at 588 when its body was replaced with a raise - no test reached it, and the nested dicts it was written for already worked. Still open and deliberately not fixed here: `offers` is copied onto the row without going through the map, unlike `notes` beside it. Nothing in this corpus carries that key CORRECTED on the day 4.3c was measured, and the correction reverses this note's own reasoning: the extractor branch for a list inside a list was reverted here as unreached, and it is reached. `_translate_value` descends a list item, `_extract_strings_from_value` dropped one, and a `notes` field shaped `[[text]]` imported clean and put the source's own script in the seed as the escapes `ensure_ascii=True` writes back out. Measured both directions on that fixture, with the whole suite green at 593 while it happened. What made the earlier measurement say `unreached` is that no fixture in the suite carried the shape - which is a statement about the fixtures and not about the walk. The branch is back, written as a plain recursion over every item rather than a per-type ladder, so the two walks cannot drift apart again by type. The uncovered LIST is what this task owns, so the assertion is a count: two strings at different depths of one nested list are both named with their own field paths, `notes.ambience` and `notes.ambience.mood`. One item would mean the coverage walk missed them and 4.3c's writer stopped the run instead. Measured by restoring the per-type ladder: it fails that test and no other, 594 passed with it deselected
- [x] 4.6 Match a re-import to the rows it already produced by source identifier, updating text and derived fields while keeping verdict and sample size, and verify a test re-imports a changed fixture over a measured row - satisfied by construction in 4.3: `existing_by_id` is keyed by `identifier`, the update path preserves `verdict` and `sample_size` and rebuilds every other field. What shipped is the assertion that keeps it true, in `test_merge_updates_text_and_preserves_verdict_and_orphaned_rows`, which already asserted the text half. Two halves were missing and both are one fixture change: the pre-existing row now carries a STALE key (`sm-chamber-01-stale`, not what `derive_room_key` makes of its identifier), so the test asserts the derived `key` moved to `sm-chamber-01` while the verdict and the sample size survived - which is also the only thing that separates a match by identifier from a match by the row's stored key, since with a key that already agreed the two are indistinguishable. The report's `updated_keys` and `created_keys` are asserted by name too; nothing in the suite read the per-destination report before (`grep -rn "orphaned_keys\|updated_keys\|created_keys" tests/ --include=*.py` returned only `test_source_manifest.py` hits for `destinations`). Measured by three breaks, each restored and each re-run with this test deselected at 590 passed: preserving the stale `key` on the update path, `created_keys.append(identifier)`, and `updated_keys.append(identifier)`
- [x] 4.7 Report a row whose source entry has disappeared upstream as orphaned without deleting it, and verify a test asserts the row survives and the report names it - satisfied by construction in 4.3, and the same test already asserted `orphaned == 1` and the orphan's verdict. Both halves of the criterion were short of that. The report only COUNTED the row, so `orphaned_keys == ["sm-vanished-row"]` is asserted now; and only the verdict was checked to survive, so the whole row is compared against the fixture dict it was written from, `sample_size` included. Measured by two breaks, each restored and each re-run with this test deselected at 590 passed: reporting `"orphaned_keys": []` while the count still came from `len(orphaned_keys)`, and re-appending the orphan without its `sample_size`. Not affected by the `gs_01`/`gs-01` key collapse: the orphan test is by identifier and the fixture's orphan key matches no incoming identifier
- [x] 4.8 Report accepted, refused, created, updated, unchanged and orphaned counts per destination, from the same data in both entries, and verify a test compares the two reports. Four of the six - created, updated, unchanged, orphaned - were already per destination in `dest_reports`, built in 4.3. `accepted` and `refused` were not: `guard_report` runs once over the whole corpus, before entries are split by destination. Attributing them is not invention - `declare_source_file` refuses any library with no destination before an entry is ever loaded (4.1), so every entry `guard_entries` sees already carries a `library` that resolves to exactly one destination via `declaration_for`. Shipped as a second per-entry pass calling `asset_guard.guard_entry` directly - not matched against `accepted_entries` by identity, since `prune_options` returns a new dict for a pruned entry - tallied into `dest_accepted`/`dest_refused` and merged into `dest_reports`, including a destination whose entries were refused whole and so never reached `entries_by_dest` before. "From the same data in both entries" was already satisfied by construction: `scripts/import_assets.py:main` prints fields read off the one dict `import_source` returns, computing nothing of its own. `grep -rn "orphaned_keys\|updated_keys\|created_keys" tests/` found only within-entry comparisons (4.3's byte-identical seed test, 4.3e's two-run comparison), never one comparing the app's dict against the CLI's output, so `test_app_and_cli_reports_carry_the_same_counts_per_destination` is new: it compares `import_source`'s returned dict against `cli_main`'s captured printed summary, overall and per destination, on a fixture with one accepted and one guard-refused entry landing on the same destination so neither count is a zero agreeing by accident. Measured by two breaks, each restored and each re-run with this test deselected at 591 passed: an off-by-one on the CLI's printed per-destination `accepted` (the two entries can diverge, and this is what would catch it), and `refused = False` in the per-destination tally (the tally itself is covered, not only the print). `python -m pytest` at 592 Reviewed by probe rather than by reading: the per-destination sums were checked against the run totals over a four-library fixture, and the fallback loop that reports a destination whose entries were ALL refused was executed by no test in the suite - replacing it with `for dest in []:` left 592 passed. Kept rather than deleted, because without it a refused entry vanishes from the per-destination report while still counting in the run total, which is the hole this task exists to close. What closed it is one fixture change on the same test - a second declared library carrying only a refused entry - plus the assertion that states the property: the per-destination `accepted` and `refused` sum to the run's own totals. Measured by that break, restored, and re-run with the test deselected at 591 passed
- [x] 4.9 Add the upload screen and its route, and verify `npm --prefix frontend run build` succeeds and a frontend test shows the report and the uncovered-string list. `POST /api/rooms/import` takes the two machine paths, runs the one import, and persists the config `import_source` mutated - the route is the registry half of 4.3's one operation, seen from outside. A missing translation comes back as 422 carrying the WHOLE `uncovered` list, because a screen that shows one string at a time sends the translator round again per string. That list is source prose: it crosses the wire and is never logged, which is what the `from None` is for. The screen is `frontend/src/views/RoomImport.jsx` on its own hash route, and the test is over `frontend/src/roomImport.js` and not over a rendered component, because this suite has no jsdom and no testing-library and every frontend test in it is a module test. Two things beyond the letter of the task, both because the route is what makes them live. `room_libraries` was missing from `ConfigIn`, and `save_config` writes `model_dump()` over config.json - so the first Setup save after an import deleted every registered library and broke the agreement check; the field is on the schema now and `Setup.jsx` carries it through the round-trip, or the schema default would resurrect candid-only and delete the imported rows just as thoroughly. And the route created the divergence its own docstring refuses: `verify_registry_disk_agreement` runs at the END of `import_source`, after the seeds are written, and its direction-2 failure arrives as a `ValueError` the route caught - so the seed stayed on disk and the entry naming it was thrown away, one more unregistered file for the next run to refuse over and no way back out from inside the app. Found by probe on a data dir carrying one unregistered seed, not by reading the diff, which had already been read. The registry half is adopted on both paths out of the import now, the refusal included; the refusal stands and stops widening what it reports. Measured by removing that one call: exactly one test goes red, `test_a_refusal_still_persists_the_registry_for_the_seed_it_wrote`, at 601 passed with it deselected. Gates re-run at review, not taken on report: 602 tests 0 failures, `npm --prefix frontend run build` built, 78 frontend tests passed

## 5. The room import

- [x] 5.1 Write the one-shot translator script that takes the source directory as a required argument with no default, and verify it exits with an error when the argument is absent. The script was already written in 4.3 - `scripts/import_assets.py`, the whole of the CLI over `backend.importer.import_source` - so what was left here was the argument contract, and it was not the one this task states: `source_dir` and `map_path` were `nargs="?"` with `default=None`, each shadowed by an unused `--source-dir` / `--map-path` flag, and a hand-written `parser.error` doing what argparse does. `grep -rn "--source-dir"` over the repo found the flags read by nothing - not a test, not the route, not the import screen - so they are gone and the positionals are required. `test_cli_refuses_to_run_without_a_source_directory` states the property, and it is not a test that cannot fail: absence exiting non-zero was already true under the old `parser.error`, so the assertions that carry it are the two that a default would break - the data dir stays empty and the config file is byte-unchanged. Measured by planting `default=str(ROOT)` on both positionals and watching it fail, then restoring. `python -m pytest` at 603
- [x] 5.2 Split the nine existing rooms into register and place, moving the capture-clause, hair, word-count and manner assertions from the stored row to the composed look in the same commit, and verify composing each of them yields the text it yielded before the split. The nine rooms shared a 300-character prefix character for character - `os.path.commonprefix` over the nine `look` fields returns the capture clause and the hair sentence and stops exactly at the place - so the split is measured rather than judged, and the directed look cuts at the same hair sentence. `look` is gone from both seed files and the field is `place`; the register lives once per manner in `data/manner-registers-seed.json`, un-ignored and tracked, which is the file `test_no_personal_data`'s gitignore rule caught as untracked before it was added. Ten rooms is one register per manner instead of ten copies of two. The join is `frontend/src/rooms.js:composeLook`, called by the picker where it used to read `room.look`, and duplicated in four lines in `test_catalogue_seed.py` rather than shared - there is no Python that needs it yet, and a backend module for one join nobody calls is the abstraction the ladder skips. What did NOT change here is the importer, which still writes `look`: that is 5.4's field and 5.4's test, and moving it early would split one task's reasoning across two. `test_every_room_carries_the_constant_half_of_the_look` is renamed and rewritten rather than deleted, per the design's own instruction: the four assertions now run on `compose_look(manner, place)` and the `manner` one reads as the permission it becomes in 6.7. The before/after check is `test_composing_the_nine_rooms_yields_the_text_they_yielded_before_the_split` against `tests/rooms-before-the-split.json`, captured from `git show HEAD:` of both seeds rather than rebuilt from the halves, which would be the thing under test asserting itself. Measured by two breaks, each restored: shortening one room's place, and editing the candid register - each fails it, so both halves are covered and not only the one that moved. `frontend/src/rooms.test.js` is new, 4 tests. `python -m pytest` at 604, vitest at 82, frontend build green
- [x] 5.3 Derive an imported room's key from the source's own identifier, normalised to ASCII and independent of any label or translation, and verify a test asserts the key is unchanged after a translation is corrected and the source re-imported. `derive_room_key` already read the identifier alone when 4.3 wrote it, so the independence half was true and untested; what was NOT true was "normalised to ASCII". It was `re.sub(r"[^a-z0-9]+", "-", ...)` over the raw identifier, which is a truncation and not a normalisation: `gs_salon_01` and the same word with an acute accent both reduce to `gs-sal-n-01`, and an identifier with no Latin in it at all returned the constant `room` - so every such entry in a library shares one key, and two rooms sharing a key is one room. Since `_find_untranslated_strings`'s own docstring records that a source identifier CAN be non-English (measured in 4.4, which is why it is not on the metadata skip list), that is a live shape and not a hypothetical. Shipped as stdlib: NFKD, then drop the combining marks rather than replacing them - replacing gives `salo-n-01`, a third spelling, instead of the `salon-01` the decomposition was for - and a 12-character sha1 of the identifier where nothing survives, which is unreadable but stable across re-imports and distinct per entry. Two tests, each measured by its own break, restored after: `test_the_key_survives_a_corrected_translation` imports one source twice with the label's translation changed between runs and asserts one row, the same key, the new label and `updated == 1` (a moved key would show as a create plus an orphan) - it fails when `room_key` is derived from `label`; and `test_a_key_is_ascii_and_two_identifiers_never_share_one` fails when the NFKD pass and the digest fallback are reverted to the bare character class and `"room"`. Neither break fails the other's test, so the two cover different things. `python -m pytest` at 606
- [x] 5.4 Store an imported room as the place alone with no manner register in its text, and verify a test composes one imported room under two manners and asserts each carries its own register and the same place text byte-identical. The importer's row field is `place` now, not `look`, which is the rename 5.2 deliberately left here so one task's reasoning was not split across two - and it is a rename, not a reshape: `theme_text` was already the place alone, since nothing in the import ever wrote a register. The join needed a Python caller and now has three, so it is one function rather than three copies: `room_registry.compose_look`, beside `load_manner_registers`, in the module that already reads the room seeds out of the same data dir. `frontend/src/rooms.js:composeLook` is the same join for the picker. One trap paid for on the way: `conftest` points `IDEVGEN_DATA_DIR` at a tmp directory for the whole suite, so `load_manner_registers()` with no argument reads an empty dir under pytest and composes the place with no register - a test asserting the hair sentence then fails for a reason that has nothing to do with what it is testing. Both test callers pass `data_dir=ROOT / "data"` explicitly. `test_an_imported_room_is_a_place_that_composes_under_either_manner` asserts the stored row carries `place` and no `look`, composes it under candid and directed, and asserts each result is its own register plus the place with the place byte-identical in both. Measured by prefixing the stored text with a capture clause in the importer, which fails it, then restored. `python -m pytest` at 607, vitest at 82, frontend build green
- [x] 5.5 Store each accepted entry's English theme string byte-identical, and verify a test compares a fixture's stored text against its source character for character. Held by construction from 4.3 - `_extract_theme_text` picks a field by whether it holds anything but whitespace and hands back the value untrimmed, on purpose and with the reason in its docstring - and untested until now. Probed before writing anything, against a theme carrying every character an import is tempted to tidy: leading and trailing spaces, an embedded newline, a tab, a backslash, a double quote, an em dash, a curly apostrophe, an e-acute and an ellipsis. The English path came back identical. The probe found one thing worth writing down: the two paths are not the same. An entry already in English is copied from the source file; an entry in another script is copied from the MAP, and `validate_translation_entry` returns `translation.strip()`, so a translation with a leading space loses it. That strip is deliberate and stays - a hand-authored map file's whitespace is an authoring artifact, not a room's words - so the test asserts the translated path against what `load_translation_map` hands over rather than against the raw JSON, which states the property that actually matters: the importer adds no edit of its own on top of the map. Also asserted, and the easy one to miss: the seed is written `ensure_ascii=True`, so the file on disk is pure ASCII escapes and the characters have to survive the ROUND TRIP - `raw.isascii()` and `not stored.isascii()` together, the second so a fixture that was plain ASCII all along cannot pass by accident. NBSP is not in the fixture: `contains_non_english` refuses U+00A0, so it can never reach a stored string in the first place. Measured by two breaks, each restored: `return val.strip()` in `_extract_theme_text` (fails on length, the leading and trailing spaces), and flattening newlines after the translate call (fails at character 49). `python -m pytest` at 608
- [x] 5.6 Store the English label and the other translated fields from the map, in seed fields distinct from the ones holding source text, and verify a test reads a seed row and asserts which values are source and which are authored. Read the criterion as written and it asks for separate field NAMES - `label` beside `source_label`. That is the wrong shape here and the corpus says why: whether a value is a translation is a property of the ENTRY, not of the field. The design's own count is 525 non-English labels out of a corpus larger than that, so `label` is the map's English for some entries and the source's own words for others, and the same holds for `notes`. Split by name and every row carries an empty half; guess from the name and the rule is right for most of the corpus and quietly wrong for the rest. Themes are not in the 731 non-English strings at all, which is what makes 5.7's "never reads a translated field" consistent - the place is source prose - but a library shipping one in another script would still translate, so the place is answered per row like the others rather than declared English by fiat. Shipped as `authored`, a sorted list of the row's own field names whose words came out of the map, collected AT the substitution: `_translate_value` takes a recorder set and `_translate_field` wraps one top-level field, so nothing is re-derived from the stored text afterwards - which is the two-calculations-that-disagree bug this repo keeps finding. Always written, empty list included, so a reader never has to tell "no translations" from "row predates the field". `test_a_row_says_which_of_its_values_are_authored_and_which_are_source` runs three entries - one with a foreign label and notes, one entirely English, one with a foreign theme - and asserts both directions on every row: a field the row calls authored differs from the entry's own value, and a field it does not call authored equals the entry's value character for character. The second half is what a merely-lucky list fails. Measured by two breaks, each restored: deriving the list from the field names (the English row then claims two translations it does not have), and dropping the recorder from the list recursion, which is 4.4's nested-list shape again - the notes translate and go unmarked. `python -m pytest` at 609, vitest at 82
- [x] 5.7 Derive `offers` from the room's English source prose intersected with the source prop list, keeping only pieces the prose names, and verify the existing offers-names-the-furniture test passes over every imported room and never reads a translated field. The source's slot is `props`, one comma-separated string per entry - `scripts/shoot_amazingdraw_method.py` records the fourteen slots and shows the shape, "supply shelves, stacked linen packs, glove boxes, rolling cart" - and a list is accepted too rather than only the shape in front of us. `derive_offers` splits it and keeps a piece only where `prose_names_piece` is true of the room's own theme text. Its author was listing what is in the scene, not promising what a photograph shows, so the intersection is the whole rule and an empty result is the correct answer: a room that offers nothing licenses no act that names furniture, which is exactly what its prose supports, and the import is not blocked by it. The comparison is ONE function now, `room_registry.prose_names_piece`, with three callers - the derivation, the seed test over the nine, the import test over every imported room - because three copies of one rule is how the room offering a piece nobody can photograph gets through the copy that is wrong. It keeps the `edge` strip verbatim: `sink-edge` is the edge OF the sink and the sentence says "the porcelain sink". `offers` is a LIST everywhere now, not a string on the nine and a list on the imported, because 5.7's derivation naturally yields zero to n pieces and a string field would mean inventing a delimiter - one reader at runtime (`ModelDetail.jsx`, display only, now `.join(', ')`) and `main.py` reads none, so the conversion is the two lines it looks like. The nine keep the requirement to offer at least one piece; only imported rooms may offer nothing. Three tests: the intersection over a room whose prose names some props and not others, one that names none, and one whose source lists no props at all; the existing rule re-run over every imported room; and a translation test that imports the same source twice with the LABEL translated two ways, one of them naming a folding screen the prose does not, and asserts `offers` is identical both times. Measured by two breaks, each restored: dropping the intersection (the stockroom then offers a rolling cart nothing can photograph), and feeding the translated label into the prose (the screen arrives). `python -m pytest` at 611, vitest at 82, frontend build green
- [x] 5.8 Compute and store the multi-body marking at import from the room's own source text, and verify a test marks a fixture whose text names other people and leaves a single-subject fixture unmarked. One field, `multi_body`, holding the WORDS that put somebody else in the frame rather than a boolean beside a list of them: the composition gate's spec says the refusal has to name the words responsible, and the boolean is that list's own truthiness, so storing both is two calculations of one fact. Empty list is the unmarked state, written on every row including the nine this project wrote, so a reader never has to tell "single subject" from "row predates the field". The vocabulary is where the judgement is. A word earns a place only when it cannot be about HER - this is a one-character studio - so plurals qualify however they are gendered (`women` and `girls` the same way as `men`, she is one person), the male terms qualify, and the role nouns qualify. What is deliberately absent is the ambiguous female singular: `woman`, `girl`, `lady`, `she`, `her`. A room's prose describes the place she is in and frequently describes her standing in it, so those words are as likely to be the subject as a second body, and marking on them would mark most of the corpus and teach the operator to turn the gate off - which costs more than the rooms it catches. The bias that is left points at over-marking on purpose: a missed room is silent until the frame comes back, a wrongly marked one names the word that did it and the operator turns the second body on or picks another room. Carries a `ponytail:` marker for its own ceiling - it is a word list, not a parser, and "an empty hall with no people in it" marks; the upgrade if that shape shows up in the corpus is a negation check on the preceding words, not a grammar. Matching is on whole words, and `manhole` is the reason - it is in the stockroom fixture and a substring rule marks a room for a hole in the road. Three tests: the marking and its words over a lobby and a dressing room with the stockroom unmarked; the ambiguous-singular decision asserted in both directions, three subject phrasings unmarked and three plural ones marked, so the omission is a stated decision and not a gap; and, in the seed suite, every shipped room's stored marking compared against what its own prose earns - the marking is computed at import and read at compose with nothing in between, so a hand edit adding a second person to one of the nine is a one-line change nothing else would notice. Measured by three breaks, each restored: substring matching (the stockroom marks on `manhole`), storing an empty list unconditionally, and editing two guests into the hallway's prose. One trap paid for again on the way: the Bash heredoc collapsed `\\b` in the regex to a literal backspace byte, which matched nothing and failed silently - see [[idevgen-invisible-byte-lesson]], built back with `chr(92)` and asserted `chr(8) not in` the file before writing. `python -m pytest` at 614, vitest at 82, frontend build green
- [x] 5.9 Store the translated authoring guidance with the room and keep it out of any composed line, and verify a test composes a room carrying guidance and asserts the guidance is absent from the result. Guidance is `notes` and the `*_anchor` fields - the only slots in this corpus that explain themselves, and the reason 2.3 translated that tier first. It is also the last text that should reach a prompt: "avoid crystal sparkle" is an instruction to a human and a description of glitter to a sampler. Shipped as ONE field, `guidance`, a dict keyed by the source's own field names, rather than as loose top-level keys - `notes` moved into it. The reason is 6.19: a picker has to show all of it, and a picker that has to know which names count as guidance is a second copy of `is_guidance_field` living in the frontend and free to disagree with the backend's. It also means 6.14's mood words drop in as one more key instead of one more rule. Always written, empty dict included, so a reader never has to tell "no guidance" from "row predates the field" - same argument as `authored` and `multi_body`. `authored` now names the dotted path, `guidance.notes`, which is the extractor's own field-path convention. Keeping guidance out of a line is STRUCTURAL and not a filter: `compose_look` joins the manner's register to the room's place and reads nothing else, so there is no step anybody can forget to apply. The test asserts both halves, because asserting only the absence passes on a row that stored no guidance at all: the stored dict equals the map's English, `authored` names exactly the two fields the map wrote (the third anchor was already English), and neither the English nor the source originals appear in what either manner composes. `is_guidance_field` carries its `ponytail:` ceiling - it is a name rule, not a per-library map. Measured by two breaks, each restored: appending the guidance to the place (the compose half fails, and so do four older tests), and dropping the collection loop (the stored half fails, with 5.6's authored list). `python -m pytest` at 615, vitest at 82
- [x] 5.10 Make the import idempotent: an existing room's text, offers and translations update while its verdict and sample size are preserved, and verify a test runs the import twice over a fixture carrying a verdict and asserts the verdict survives. The merge itself was already there from 4.3 and 4.6 - matched by source identifier, verdict/sample_size/manner carried over, orphans retained - so what this task was actually missing was the measurement that the two halves stay pulled apart, and the existing test only imported once over a hand-written seed. The two halves pull against each other: everything the SOURCE owns has to move on a re-import, or a corrected translation never reaches the app; everything THIS project measured has to stay, or an import quietly deletes frames somebody paid for. `test_reimport_updates_the_room_and_leaves_its_measurement_alone` runs the real sequence - import, then a verdict written onto the row the way a pass writes one, then a second import over a reworded theme, a different prop list and a corrected map - and asserts both halves on one row: place, label, `guidance.notes` and `authored` move, `offers` RE-DERIVES from the new prose (5.7's derivation is not frozen at create), and verdict and sample_size are untouched. A third run over unchanged source is asserted BYTE for byte on the file, not by the report's `unchanged` count: a merge that rewrites the same row with its keys in a new order reports unchanged and still makes a diff every run. Measured by two breaks, each restored: dropping verdict and sample_size from the preserve list, and preserving `place` and `offers` alongside them - the second is the mistake worth naming, since "preserve the existing row" reads reasonable and turns the importer into a writer that only ever creates. One trap paid for again: the Bash heredoc collapsed the escape in `+ "\\n"` to a real newline and split a string literal across two lines, see [[idevgen-invisible-byte-lesson]]. `python -m pytest` at 616, vitest at 82
- [x] 5.11 Skip and report an entry whose English theme string is empty rather than inventing text, and verify a test asserts the skip and the report. Read and ticked: the code and both tests were already standing. `importer.py` filters on `if not theme` before translation coverage is checked, so an empty entry is neither refused nor written and its strings cannot take an upload down; the report carries `skipped_empty` and `skipped_empty_identifiers`, and `scripts/import_assets.py` prints the count. What made the tick worth the run rather than a reading is that the entries this skips are real: every source file yields a header entry for its own root dict, which the guard ACCEPTS, so without the skip 13 of general's 204 accepted entries become rooms with no text. `test_empty_header_entries_skipped_and_reported` asserts the identifier by name, and the guard test asserts `accepted == 2` against `written == 1` deliberately, which is what keeps the two roads into "not written" told apart. Measured by one break, restored: `if not theme` to `if False` fails nine tests across two files
- [x] 5.12 Import one source library at a time into its own seed file, largest last, and verify `python -m pytest` is green after each library lands. An operation, not a code change: nothing tracked moved, because the seeds land in the ignored data directory and the registry half lands in the ignored `config.json`. Six libraries, ascending by file size, each through `scripts/import_assets.py` against the untracked corpus and its translation map, with the full suite run after each - 626 passed, six times. **387 rooms landed** and every count reconciles against the inventory: medical 34 of 34, special 16 of 16, workplace 75 written and 1 refused of 76, sm 71 written and 3 refused of 74, general 191 of 191. The refusals are the guard doing entry-level work inside an accepted library - `workplace-school-teacher-office` and two `sm-urban-petplay` campus entries - which is exactly the split 4.1 declared: the library name says where a file lands, never whether a given entry is carried. `school_scenes` refused all 93 entries, wrote no file and registered nothing, which is the pair holding rather than a half-import to clean up. Probed after the last library rather than read: `verify_registry_disk_agreement` passes, the four registered seeds hold 396 rows with the nine shipped candid rooms, not one byte over ASCII in any seed, and `GET /api/rooms` serves all 396 with verdicts present on the nine and absent on the 387 - absence is the default, and no import writes the verdict store. Two entries were named and left alone: `general-wedding-backstage-dressing` at weight 93 and `general-subway-door-crush` at 95 against a library median of 3, which is 6.16 reporting and never acting. `studio-softbox` is reported as a verdict with no room in every one of the six runs, which is 6.6: the studio's room is ours, no imported library carries it, and the verdict is named and never deleted

## 6. The room picker

- [x] 6.1 Keep the project's own rooms tracked and build-time, write imported rooms to an untracked path, and verify `tests/test_catalogue_seed.py` still passes and the frontend builds on a checkout with no import run. Both properties were already TRUE and neither was ASSERTED, which is the whole of this task: `data/*` is ignored with an allowlist of `!` lines, so an import destination is untracked by default, and the frontend's four build-time seed imports are the nine rooms, directed's look, the catalogue and the registers - all tracked. What was missing is that nothing holds either one still. `test_tracked_data_files_are_named_by_gitignore` asserts the direction "nothing tracked under data/ is unnamed"; it stays green the day somebody writes `!data/general-scenes-rooms-seed.json`, which is exactly how 428 rooms of somebody else's wording get published. `test_an_import_destination_is_neither_tracked_nor_bundled_into_the_build` asserts the other direction, and both halves of it fail silently in the direction that matters: a tracked destination publishes prose with nothing going red, and a build-time `import ... from '../../data/<imported>.json'` builds on the machine that ran the import and breaks every fresh clone - the failure `tests/test_catalogue_seed.py` exists for. Destinations are read off `SOURCE_LIBRARIES` rather than off a filename pattern, so a library added to the manifest is covered the day it is added and not the day somebody remembers this test; `git check-ignore` is asked rather than the `!` lines parsed, because the question is what git does with a path that does not exist yet. Measured by two breaks, each restored: an import of `general-scenes-rooms-seed.json` added to `ModelDetail.jsx`, and a `!` line for that destination appended to `.gitignore` - the second also reddens the drift test, which is the pair working. `python -m pytest` at 617, `npm run build` green with no imported seed on disk
- [x] 6.2 Compose the manner's register with the room's place when a room is picked, adding no register to a hand-written look, and verify a test asserts a look filled from one room under two manners differs only in its register. The composing was already done by 5.1's split; what had no test was the picker's own branch, which lived inline in an `onChange` where nothing could reach it. Shipped as `lookFromRoom(manner, rooms, key)` in `rooms.js` - the one function that decides whether a hand-written look is overwritten - returning the composed look or NULL, and null is not an empty look: the picker's first option is "start from a measured room", and choosing it back has to leave text somebody typed exactly as they typed it, where an empty look would clear the textarea. One consumer, so it is an extraction and not an abstraction; 6.15's draw is the second. The test asserts the strong form of the difference and not the weak one: the two looks differ, AND the two with their registers sliced off are the same string, AND that string is the room's place character for character. The weak version - the two differ - passes on a compose that reworded the place as well, which is exactly what the split exists to prevent. The hand-written half is asserted through the same function: a key no room carries, an empty key, an empty room list and a typed sentence all return null, so no text that did not come from a room can leave here wearing a register. Measured by two breaks, each restored: composing an empty place instead of returning null (the picker then blanks a typed look), and rewording the place inside the compose (the byte-for-byte half fails while the weak half stays green). `npx vitest run` at 85, `npm run build` green, `python -m pytest` at 617
- [x] 6.3 Say that a look carries another manner's register when the session's manner changes, offering a refill and rewriting nothing, and verify a frontend test asserts edited text survives a manner change. The order on the screen is what makes this real: the room picker sits above the manner select, so filling a look and then changing the manner is the ORDINARY sequence, and until now it left the session's look opening with the other manner's register and nobody said so. `refillLook(manner, look)` returns the manner whose register the look carries and the same look wearing this session's instead, or null when it carries the right one or none at all - a look somebody typed is not wrong, it is theirs, and an offer there is a screen telling the operator their own sentence is a mistake. It is an OFFER: the value is returned, the notice names both manners, and one click writes it. Nothing happens without the click, because a manner change that silently rewrote the look is how an edit made after the room was picked disappears. What is swapped is the register alone - everything after it survives character for character, the room prose and whatever was typed into it since - which is why the refill slices the old register off rather than recomposing from the room. Four tests: the offer names candid and hands back the directed composition; an edit appended after the fill survives the swap; nothing is offered for the session's own register, a hand-written look, an empty look or undefined; and asking leaves the look it was asked about untouched. Measured by two breaks, each restored: recomposing from the room's half of the text instead of keeping the tail (the edit is dropped, and only the edit test sees it), and dropping the `m !== manner` guard (every filled look is then told its own register is foreign). `npx vitest run` at 89, `npm run build` green
- [x] 6.4 Serve imported rooms at runtime through a route, returning an empty list and a stated reason when the library is absent, and verify a frontend test renders the picker with the route empty. `GET /api/rooms` over `room_registry.available_rooms`, which is a second reader of the registry on purpose and NOT a flag on `load_room_libraries`: that one raises on a registered library whose seed is missing, which is right where an import is being verified and wrong for a screen. Untracked seeds make "the registry names a file that is not there" the ordinary state of a fresh clone, a second machine and any checkout where nobody ran the import, so raising there is a picker that will not open instead of a picker holding the nine tracked rooms. Three ways to contribute nothing, each with its own sentence - no seed on disk, switched off, unreadable - and the sentence names the SEED FILE and never its path: a machine path in an API response is the same leak as one in a tracked file, one hop further out, and the test asserts `tmp_path` appears nowhere in the body. On the frontend the tracked half stays a build-time import and the served half is fetched, joined by `pickerRooms(builtIn, served)`, which dedupes by key because the registry's DEFAULT entry is the nine tracked rooms themselves - the route hands back what the bundle already carries, and without the dedup the picker lists all nine twice. The tracked copy wins, being the one the build was tested against. A route that answers nothing, undefined or null all leave the picker exactly the built-in list, which is 6.4's own criterion. Measured by two breaks, each restored: raising instead of stating a reason for a missing seed, and dropping the dedup. `python -m pytest` at 618, `npx vitest run` at 92, `npm run build` green
- [x] 6.5 Store verdicts, sample sizes and the manner measured under in a tracked file keyed by room key, carrying no source prose, and verify a test asserts the file holds no room text. `data/room-verdicts-seed.json`, tracked, `{room key: {manner: {verdict, sample_size, note}}}` - keyed by manner because being ALLOWED in a manner is not being MEASURED in one, and a room verified candid says nothing about directed. Starts empty; 6.10 converts the nine. `load_room_verdicts` reads it the way the registers are read - absent or unreadable is no verdicts, which is the state of a checkout before anybody shoots one - and `available_rooms` joins each room to its own verdicts so the loader has a reader from the day it exists. The prose rule is asserted rather than trusted: six consecutive words of any room's place appearing in any stored string, under any field name, is the room's sentence copied. Six because shorter runs collide honestly - "on the bed" - and 6.10's note naming the run that measured a room is allowed to name what is in it. The detector is exercised on invented rooms in BOTH directions inside the test, because the real store starts empty and a rule that only ever reads an empty file is a rule nobody has run. Found a real bug on the way, and not by reading: the suite went red the moment the file existed. `is_room_seed_file` is "ends in -seed.json and contains room", so the verdict store read as a room LIBRARY - unregistered on every checkout, which makes direction 2 of `verify_registry_disk_agreement` refuse every import while the file sits on disk. Fixed at the one function both readers go through rather than at the test, and asserted there, because a rename of the file would otherwise take the exclusion with it silently. Measured by two breaks, each restored: a room's place planted in a note, and a `place` field added to a record. `python -m pytest` at 619, `npx vitest run` at 92
- [x] 6.6 Report a verdict whose room is absent as orphaned without deleting it, and verify a test asserts it survives an import that no longer carries that room. The split 6.5 made is what turns an orphan from an exception into the ordinary state: the store is tracked and the seeds it measures are not, so a clone, a second machine, a library switched off and a seed somebody moved all leave verdicts pointing at rooms that are not here, and deleting on any of those throws away frames that were actually shot. One computation, in `available_rooms` where the rooms and the verdicts are already joined, and the importer's report asks it rather than keeping its own answer - a run's own destinations are the wrong question, because a verdict is orphaned by the rooms that ARE here and not by the ones this run happened to touch. Two absences are told apart on purpose and the test asserts both: a row the upstream dropped is RETAINED by the merge from 5.10, so its verdict is NOT orphaned - the room is still here, it is only no longer upstream - while a verdict for a room no registered library carries is. Reported in three places and acted on in none: the CLI line, a muted line on the import screen, and the `/api/rooms` payload. The never-deleted half is asserted on the bytes and not on the keys, because a rewrite that preserved every key and re-indented the file is still an import that took the store as its own. Measured by two breaks, each restored: the import deleting the orphaned keys after reporting them (the byte comparison fails), and reporting every verdict as orphaned instead of the absent ones (the switched-on assertion fails while the switched-off one stays green). `python -m pytest` at 620, `npx vitest run` at 92, `npm run build` green
- [x] 6.7 Replace a room's single `manner` with the set of manners it allows, defaulting to all, and verify a test asserts an imported fixture allows every manner and the studio row allows only the manners where someone is photographing her. The field kept its name for one commit too long and its MEANING changed with the split: `manner` said which register was fused into the text, `manners` says whether the place makes sense under a manner at all. Stored as a RESTRICTION and not as a membership - the nine rooms and every imported row carry `[]`, and empty is every manner. A frozen `["candid", "directed", "selfie"]` reads identically today and silently excludes every existing room from the fourth manner the day 8.5 writes one, so the test asserts the empty list allows a manner nobody has written yet. The studio is the one restriction anybody has a reason for, and the reason is stored beside it in `manners_reason` because the spec requires the reason and a bare `["directed"]` is a rule whose why lives in a commit message. The importer preserves a hand-written restriction across a re-import the way it preserves a verdict - both are this project's judgement about a room the source owns the text of - which also simplified the preserve loop, since `manners` is now always present on the new row. Measured by one break, restored: the import dropping `manners` from the preserve list resets a hand-written `["directed"]` to `[]` on the next run. `python -m pytest` at 621
- [x] 6.8 Filter the picker by the allowed set rather than by equality, and verify a frontend test lists a room allowed everywhere under each manner and hides the studio under the others. `roomAllows(room, manner)` in `rooms.js`, and the picker's filter is that call - one reader of the rule, on the side that offers the rooms, and no Python twin: a second copy is a rule that can disagree with itself, and nothing on the backend asks the question yet. Shipped with 6.7 in one commit because splitting them ships a broken picker: the moment the seeds carry `manners`, an equality filter on `r.manner` lists nothing at all. The test asserts the shipped ten under each of the three manners rather than a fixture pair - the bedroom under all three, the studio only under directed, and the counts 9 and 10 - because the fixture cannot go stale and the seeds can. Measured by one break, restored: `roomAllows` reading an empty list as "nothing allowed" empties the picker under every manner, and both the unit test and the listing test see it. `npx vitest run` at 95, `npm run build` green
- [x] 6.9 Add a verdict and a sample size per manner to the room seed shape with an unverified default, and verify a test asserts every stored verdict is one of the catalogue's words and carries a sample size. The shape is the store 6.5 built, not a field on the row - `{key: {manner: {verdict, sample_size, note}}}` - and what 6.9 adds is the vocabulary and the rule tying the word to the count. The words are the catalogue's own three, `unknown`, `verified` and `dead`, and they are BOUND to `db.cell_state` rather than written out beside it: two lists of the same three strings in two files drift the first time somebody adds a fourth, and silently, because the picker keeps rendering a word no rule can produce. `unverified` is prose and is deliberately not one of them - a second vocabulary for one question means every reader has to know which of the two it is holding. The default is ABSENCE: a room with no record under a manner has not been measured under it, and writing that out 428 times is a file of zeroes. The stored word must also be one the counts can produce, so `verified` at n=9 fails the same way a hand-typed reading would. Measured by two breaks, each restored: a fourth word added to `VERDICT_WORDS` (the binding test names the extra), and a nine-room row flipped to verified. `python -m pytest` at 623
- [x] 6.10 Convert the nine rooms' free-text verdicts to that vocabulary against the manner they were measured under, at the sample size the text states and no higher, keeping the original sentence as a note, and verify a test asserts none of the nine is stored verified and each keeps its sentence. Nine `unknown` under candid: eight at n=1 from `built 1/1`, and `bedroom-night` at n=0 because `in use since session 351` states no count at all - the sample size the sentence states and no higher, taken literally. The studio was the one real decision and it went the other way, asked and answered rather than guessed: its sentence states ten seeds and says the softbox, paper roll and reflector are all built, which is ten judged and ten arrived, `verified` under directed by the same rule a cell is read with. design.md's consequence paragraph said every room would show unverified on the day this shipped and now says which one does not. The prose is not deleted - it is the note, because the vocabulary cannot say which run measured what. The row loses the field entirely, which is the half of this task nobody asked for and the half that matters: a `verdict` string left on the row is the second answer the split exists to prevent, and it would answer with whatever it was carrying the day the store was written. That cost two 4.x/5.x tests their fixtures - the merge test now preserves `manners` and `manners_reason` across a re-import, and 5.10's measurement lives in the store and is asserted byte for byte. `python -m pytest` at 623, `npx vitest run` at 95, `npm run build` green
- [x] 6.11 Show a room as unverified for a session whose manner it has no verdict under, and verify a frontend test asserts a room verified in one manner is not shown as verified in another. `verdictFor(room, manner)` in `rooms.js`, reading the store per manner and answering `unknown` at n=0 for an absent record - which is what absence MEANS, not a gap being filled in. Two sources joined in the one function: a served room arrives with its verdicts attached by `/api/rooms`, a tracked room is in the bundle and its verdicts come from the same tracked file the route reads, so the two cannot disagree. The studio is what makes this testable rather than hypothetical - the only measured room in the library, verified under directed and unmeasured under the other two. Measured by one break, restored: falling back to the room's first verdict when the manner has none takes the studio's directed 10/10 and shows it under candid, which is the exact failure of presenting 428 unmeasured pairings as measured. `npx vitest run` at 99
- [x] 6.12 Show the verdict in the picker so verified and unverified rooms are visibly distinct, and verify `npm --prefix frontend run build` succeeds and the frontend test covers the distinction. A select holds text and nothing else, so the distinction is words: `verified, 10 judged` against `not measured yet`. The sample size is always shown with the word because `verified` on its own is the free text this whole store replaced - it says nothing until it says out of how many - and an unmeasured room says so in plain words rather than showing the catalogue's `unknown`, which reads as a fault in the app instead of a room nobody has shot. `roomOption(room, manner)` builds the whole line - label, what it offers, what it measured - in `rooms.js` rather than inside the JSX, for the same reason `roomImport.js` exists: the suite has no jsdom, and what is worth asserting is the string and not the paint. Measured by one break, restored: printing the bare verdict word drops both the count and the plain-words half, and three tests see it. `npx vitest run` at 99, `npm run build` green, `python -m pytest` at 623
- [x] 6.13 Store the source's tags on the room and read the picker's tag filter from them, and verify a frontend test filters a fixture by a tag its source entry carried. Taken whole, no threshold: a tag used once still finds its room. Stored translated - a tag in the source's language is a filter nobody here can type - and `authored` names the field when the map wrote it. NOT intersected with the prose the way `offers` is, which is the one thing worth saying about the difference: a prop the sentence never names is furniture no photograph contains, while `private` is true of a bedroom whose sentence never uses the word. The filter's options come from `allTags(rooms)` rather than from a list in the frontend - the vocabulary is 239 tags long and belongs to the source, so a copy here goes stale on the next import - and the select is hidden entirely when no room carries a tag, which is a fresh clone and every checkout where nobody ran an import. Measured by one break, restored: storing the tags without passing them through the map leaves the source's own script in the row and in the filter. `python -m pytest` at 624, `npx vitest run` at 101, `npm run build` green
- [x] 6.14 Store the source's mood words with the room's authoring guidance and offer no filter over them, and verify a test asserts they are readable and absent from any composed line. One more name in `is_guidance_field` and nothing else, which is what 5.9 built the `guidance` dict for: the mood words land under the source's own field name beside the notes and the anchors. They get no filter because 220 unique with 165 used exactly once is 165 filters returning one room each - noise shaped like a feature - while as something a human reads while writing a line they are the same material as a note. The name rule covers `mood` and any `mood_*` slot, and carries the same ponytail ceiling the rest of the rule does: a library that calls it something else stores it as an ordinary field, and the upgrade is another name here rather than a schema. Absence from a line stays STRUCTURAL: `compose_look` reads the register and the place and nothing else, so the test asserts both directions - the stored English is there AND neither it nor the source original appears in what either manner composes. Measured by one break, restored: dropping the mood clause from the name rule leaves the words as a loose top-level key and the guidance read fails. `python -m pytest` at 624
- [x] 6.15 Add the weighted draw at session creation, over the rooms the session's manner allows, filling the look once and showing the drawn room's verdict, and verify a test asserts the draw is not repeated when the session is reopened. The source's weight is stored on the row here, because this is the task that gives it a consumer - a room nobody weighted deals at one, and a weight that cannot be read as a number is one too rather than a refused room. `drawRoom` deals over the rooms the manner allows, weighted, and `openingLook` is the guard that makes it happen once: it returns null whenever the session already carries a look, so a re-render, a reopened session and a look somebody typed are all left exactly alone. The call sits in the `startSession` HANDLER and never in render, which is the failure the guard is really against - a draw in render deals a new room on every keystroke in the form. Weight zero is kept meaningful: a room the picker offers and the draw never deals. The test uses a rand that walks the interval rather than a stub, since a draw that always returns the first room passes every single-value assertion. Measured by two breaks, each restored: dealing every room at one (both weight assertions fail) and dropping the already-has-a-look guard (the once test fails). `npx vitest run` at 104
- [x] 6.16 Report an imported weight far outside the range the library otherwise uses, naming the entry, and verify a test asserts the two outlying fixtures are named and their weights are stored unchanged. Ten times the library's MEDIAN, either way - not the mean, which one entry weighted 500 drags far enough to hide behind while naming the ordinary rooms instead, and that is exactly what the deliberate break showed. Reported and never corrected: normalising a number quietly is an import that lies about what it read, and the whole value of the line is somebody noticing before a session's worth of frames comes back in one room. A library of fewer than three rooms reports nothing, because two rooms have no middle to be an outlier of - whichever is bigger would name the other. A library that weights nothing at all lands on one across the board and reports nothing, which is the second test: a default that made the report fire on every room would be worse than no report. Named by identifier in the run report, the per-destination report, the CLI and the import screen, and the test asserts no entry text reaches any of them. ponytail ceiling recorded on the rule: a factor against the median rather than a distribution fit; the upgrade is a quantile the day a library arrives with a real spread. Measured by two breaks, each restored: the mean in place of the median, and clamping the stored weight to the library's range. `python -m pytest` at 626, `npm run build` green
- [x] 6.17 Add text and tag filtering to the picker, matching text against both the translated label and the English source prose, and verify a frontend test finds one fixture room by its label and another by a word inside its prose. The tag half shipped with 6.13; what is added here is the text box and `filterRooms`, which is the one place manner, tag and text are ANDed together - three filters spread over three call sites is three chances to forget one. The label and the prose answer different questions and both are searched: the label is a translation somebody wrote and is what the operator reads, the prose is the English the photograph is actually made of, so `mirror` has to find the room whose sentence has one whatever its label says. Not searched: the tags, which have their own control, and the guidance, which is advice about the room rather than a description of it. Measured by one break, restored: matching the label alone loses the prose room and the combined tag-and-text case with it. `npx vitest run` at 107
- [x] 6.18 Keep the session's current room reachable regardless of the active filter, and verify a frontend test filters it out and asserts it is still shown as the current selection. One clause in `filterRooms`, and the reason it is not cosmetic: the select's value is now the current room's key rather than the empty string, and a select whose value is not among its options renders BLANK - so a filter that hid the current room would have the screen saying the session has no room while its look is full of one, with picking something else the only way out. The room the session is using is tracked as a key in the picker's own state, set by the draw at creation and by every pick since; 7.2 is what will carry that key onto the session itself. The test filters the current room out three ways at once - wrong tag, wrong text, and a manner it is not allowed in - and asserts it is still listed, exactly once when it also passes the filter. Measured by one break, restored: dropping the clause loses the pinned room in that test and in no other. `npm run build` green
- [x] 6.19 Make a room's authoring guidance readable from the picker without composing it, and verify a frontend test shows the guidance for a fixture room. `guidanceLines(room)` flattens the row's `guidance` dict into one line per source field, list values joined, blank values dropped - and it reads the FIELD NAMES OFF THE ROW, which is why 5.9 shipped guidance as one dict instead of loose top-level keys: which slots count as guidance is `is_guidance_field`'s decision, and a copy of that rule here would be free to disagree with it. Shown under the room the session is using, so it is read while the line is being written, which is who the entry author wrote it for. Never composed, and structurally so: `composeLook` joins the register to the place and reads nothing else, and the test asserts the words are absent from what both manners compose. Measured by one break, restored: rendering a list value with `String()` prints `tender,hushed` as an array's toString and the expected lines fail. `npx vitest run` at 109
- [x] 6.20 Confirm selecting a room fills only the look, and verify a test asserts the session's wardrobe, shots and settings are unchanged. It was already true and nothing held it still, which is the whole task: the spread lived inside an `onChange` where no test could reach it. `pickRoom(session, rooms, key)` is that spread with a name, and the assertion it buys is IDENTITY rather than equality - the wardrobe, the shots and the settings come back as the same objects, so an edit that ever rebuilt one of them fails here instead of being noticed by somebody whose shot list reset itself. The key set is asserted too: a room that quietly added a field would pass every per-field check. Choosing the first option back returns the very same session object, which is 6.2's rule about a typed look, now visible in the return type rather than in a null check at the call site. Measured by one break, restored: copying the shots array on the way through - a deep comparison would never see it, and the identity assertion does. `npx vitest run` at 109, `npm run build` green, `python -m pytest` at 626

## 7. The compose gates

- [x] 7.1 Record the key of the room a session's look was filled from - a column on `session`, empty for a hand-written look and for every session that exists today - in `SCHEMA` and in `_migrate` together, and verify `tests/test_db_migrate.py` opens a database written before the column and asserts the migrated session reads an empty key. `room_key TEXT NOT NULL DEFAULT ''`, provenance and nothing else: `look` still carries the whole text, so a session composes identically whether the key is set or empty, and 7.2's detach is a column write that touches no word of the look. Not a foreign key - a library can be unregistered and a session whose room is gone still has to open. **Nothing is back-filled and nothing can be**: by storage time the look is one block of text, and matching it against the catalogue to decide which room wrote it would invent provenance for a look somebody typed. The test asserts the look byte for byte beside the empty key, because the failure worth catching is not a missing column - the next query raises - but a migration that adds the column by rebuilding the table and rewrites the text on the way. It also asks `SCHEMA` on its own, over a bare in-memory database with no migration behind it: everything else in the test passes on a column that lives only in `_migrate`, since `db.connect` runs both, and `session.origin` is already in the tree that way. Measured by two breaks, one per half, each restored: the migrate branch deleted, and the SCHEMA line deleted - the same test fails both times, and only that test. `python -m pytest` at 627, vitest 109
- [x] 7.2 Carry the key through the picker and the compose payload, and offer the operator a way to detach it that keeps the look's text and clears the key, and verify a test asserts picking a room stores its key, replacing the room replaces it, detaching clears it while the text stays byte-identical, and a session with no key composes unrefused. `room_key` on `SessionIn` and on `SessionPatch`, written trimmed with **no check that the room exists** - a library can be unregistered after a session was filled from it, and refusing the write would leave the operator unable to detach the one key they can no longer look up. The text surviving a detach is STRUCTURAL and not a rule the route remembers: `look` is deliberately not a field `SessionPatch` accepts, so nothing on the detach path can reach the words. The clone carries the key too, on the argument its own comment already makes for tags - the copy's look is the source's look byte for byte, so the room that filled one filled the other. Provenance only: nothing downstream reads the column, which is what the compose assertion pins - the day somebody makes the key a condition of a strict draw, that line fails. On the screen the three transitions became `roomChoice` in `rooms.js` rather than a branch in the onChange, the same move 6.20 made and for the same reason: detach is the one that gets written wrong, and lifting it is what lets the test assert IDENTITY - the detached session is the same object, so no edit slipped into the words. A key no room carries moves neither half, which is the stale-select case a library unregistered while the form was open would otherwise record. `SessionView` grew the line that says which room filled the look and the button that detaches it - the KEY and not a label, because that screen does not load the room library and a session outlives the library it was filled from. Measured by six breaks, each restored: the PATCH ignoring the field, create dropping it, the clone dropping it, the detach returning a copy instead of the session, the detach clearing the look, and a stale key accepted. `python -m pytest` at 629, vitest 114, frontend build green
- [x] 7.3 Refuse a compose whose room is marked multi-body when the run has no second body declared, naming the room and the words responsible, and verify a test asserts the refusal text carries both. One check, in `_draw_n_trio_shots`, which is the body both run endpoints share - and a RUN-level refusal rather than a per-photograph one, because the look composes the room into every photograph of the session and no act pool can undo that. `with_him` is the declaration; the room's own words come from `multi_body`, derived once at import and read here by key. Read from the CATALOGUE and never from the look: the look is the operator's from the moment the room filled it, and re-reading it here would answer a question about words that may no longer be in it - which is also why a detached session passes, no key being no claim. `_room_for_session` answers None for three states and they are all ordinary: no key, an unregistered library, a key nobody carries any more. A session held hostage by a seed file somebody stopped registering is worse than an ungated run. The refusal names the room AND the words, asserted separately, because either alone is unusable - the room without the words is a paragraph the operator wrote with no indication which half offended, and the words without the room name a problem with no location. Measured by four breaks, each restored: the gate removed, the gate ignoring `with_him`, the words dropped from the message, and the key never read
- [x] 7.4 Compose the same room unchanged when the run declares a second body, and verify a test asserts the composed room text is byte-identical to the stored source text. The half that stops 7.3 from being implemented as an editor: the gate is a yes or a no and never a rewrite. A test that only checked the run was accepted would pass on a gate that "handled" a crowded room by dropping the clause about the nurse, and the session would then shoot a different room than the one the operator picked and the verdicts were measured on. So the room's own sentence is asserted inside the composed prompt, byte for byte. Measured by a break that shortens the look on its way into `compose_shot`, restored. **The first attempt at that break was a no-op and passed**, twice: `session["look"] if use_look else ""` appears at three sites and a `replace(..., 1)` hit the wrong one, and the correction `... else ""[:40]` slices the else branch alone. A break that does not fail is the same evidence as a test that cannot fail - see [[idevgen-test-that-cannot-fail]]
- [x] 7.5 Add the configured room word budget with a conservative documented default, refusing over-budget rooms with the count and the budget in the message, and verify a test asserts both numbers appear. `room_word_budget` on `ConfigIn` - it has to be there or the next Setup save deletes it, the same trap `room_libraries` documents - defaulting to `ROOM_WORD_BUDGET = 120`, named ONCE and read by the schema, by the gate and by the test. **The default refuses nothing and is meant to.** Session 391 kept a camera 3/3 on a 242-word line and lost it on a 224-word one; session 394 shot one line twice, once as written and once with 87 to 187 words of whole blocks deleted, same seed, everything else byte-identical, 9/16 against 9/16. Length is not the mechanism it was taken for, so a gate tuned as though it were would refuse rooms for a reason nobody measured. The imported corpus is 396 rooms, median 17 words, longest 89 - 120 sits above all of it, which makes this a tripwire for an absurd input until 7.9 sets the real number. Counted on the room's stored text and never on the look, for 7.3's reason. Both numbers in the message, because a limit without the measurement is untunable. Measured by three breaks, each restored: the gate removed, the count dropped from the message, and the default tuned to 40. **The third break passed the first time it was tried**, because the test read `main.CONFIG.get(..., 120)` - its own copy of the number, green whatever the app enforces. That is what put the constant in one place and the test on it. See [[idevgen-test-that-cannot-fail]]
- [x] 7.6 Assert no silent shortening: every successful compose carries the stored source text byte-identical, and verify a property-style test over every seeded room. Over the ten rooms the build carries, NAMED rather than globbed: `data/` on a working machine also holds the imported libraries, which are untracked, so a glob reads a different number of rooms on every machine and fails on somebody's private import. The failure it is written against is a composer that shortens SOME rooms - the long ones, the ones with a semicolon, the ones whose prose ends without a full stop - which a test over one hand-written fixture never sees. A room that reaches the prompt one character short is a room shot under text nobody wrote, carrying a verdict that no longer describes it. Measured by a break that drops five characters of the look on its way into `compose_shot`, restored - **the first attempt dropped ONE character and passed**, because `compose_shot` re-adds the full stop it removed. A break has to be bigger than the thing that would repair it
- [x] 7.7 Refuse a run before anything is queued when its room fails a gate, leaving no partial shots, and verify a test asserts the shot count is unchanged after a refused run. UNCHANGED and not zero, which is the whole task: `db.run` commits per INSERT, so the way this breaks is a gate inside the queueing loop - k photographs land, the k+1th refuses, and the operator is handed a 422 over a run that half happened. An `== 0` on an empty session passes on exactly that code, because the first photograph is the one that fires the gate. So the session already carries two photographs, one written and one composed, and both gates are then fired from it in turn - "refuse before queueing" is a property of the check's POSITION, and a second gate added below the loop would pass a test that exercised only the first. Measured by moving the multi-body gate below the insert, restored: two tests fail. **Two earlier attempts at this test were wrong and both looked green.** The first break put the refusal at the TOP of the loop body, which still queues nothing - a partial write has to be written as a partial write. And the test itself asked for a run of five against a pool of one, so its 422 came from the draw's own pool check and never reached the room at all; the pool is five trios now and the refusal is asserted BY NAME, which is what tells the two 422s apart
- [x] 7.8 Add the ahead-of-time report listing which rooms a planned run would refuse and why, and verify a test asserts it queues nothing. `POST /api/rooms/preflight`, taking the one declaration a room gate reads - the count, the candidates and the wardrobe arc decide nothing about a room. Both gates moved into `room_refusal` first, and the report and the run now call the SAME function: a report that recomputed the rules is a second opinion, and the day the two disagree is the day the operator picks a room the report cleared and the run refuses. The test asserts that agreement directly - the reason string from the report is compared to the run's own 422 detail, not merely to a substring. Queueing nothing is asserted over the whole shot table rather than one session, because the report is not sent from a session and a call that queued something would queue it somewhere the test does not know to look. The operator's problem it solves: today you learn a room is unshootable by sending the run and reading the 422, which is fine for one room and useless for a picker with 396 in it. Measured by three breaks, each restored: the report recomputing the rule in its own words, the report queueing a shot, and the report ignoring `with_him`
- [x] 7.9 Write the measurement arm that varies room length alone against a fixed camera row, run it, and verify the budget default is set from its result and the number is recorded in design.md's Open Questions. `scripts/shoot_room_length.py`, sessions 395-400 on premium. The instrument is the project's one CALIBRATED cell - session 382's anchor, `side-view` / `wall-facing-forearms` / `crop-full-body` - composed through the app and not written verbatim, because a verbatim line leaves `shot.components` empty and the judge endpoint refuses it: a verbatim arm can never be judged against the catalogue's own vocabulary. Six arms, ten photographs each, wardrobe muted throughout; the room moves and nothing else does. The ladder is cut from ONE room, `general-rooftop-laundry`, by whole clauses - the corpus's own long rooms are abstract chambers whose text describes her body, and those would have fought the act rather than measured it. Judged blind on the camera slot's `position` axis at five passes, two frontal controls in every run, `front` 2/2 every time. **0w: 7/10. 18w: 8/10. 53w: 9/10. 84w: 10/10. 176w: 10/10. Studio control at 48w: 5/10.** The trend runs UPWARD with length and the control is the worst arm of the six, which is the opposite of a length cost - the third session running to hunt this effect and find something else. `ROOM_WORD_BUDGET` is set to **200** from that: above the longest length measured good, which is already twice the longest room in the corpus. It refuses nothing anybody has, and it stays as a tripwire because a refusal beats a silent truncation. Nothing was posted to the cell: all six arms share one trio, manner and checkpoint, so `--post` would have merged them into a single cell and destroyed the very comparison. Recorded in design.md's Open Questions with the caveat that matters - **read by eye off the contact sheet all six arms looked side-on 10/10 and they are not.** Several frames are three-quarters from behind and read `shoulder`; the eye was wrong in the direction that would have hidden the control arm's own weakness. See [[idevgen-readings-from-photographs]]

## 8. Perspective mining

- [x] 8.1 Write the curated cut map at an untracked path beside the translation map - one entry per source identifier naming the substring that becomes the camera, the one that becomes the act and the one that becomes the room, omitting what becomes nothing - and verify a test asserts every cut is a substring of its source entry and that an entry missing from the map stops the import instead of being guessed at `backend/cut_map.py`, keyed by source identifier, three slots and no fourth - a wardrobe clause is named by none of them because a mined fragment does not enter this project's wardrobe catalogue. An omitted slot is how the map says the entry carries no such part; an EMPTY one is refused rather than read as absent, and so is a key outside the three, because a misspelt `camera` read as "becomes nothing" is a cut lost silently and nothing downstream ever asks for it again. The substring check compares against each of the entry's own strings WHOLE and never against them joined: joining invents a boundary, and a cut running from the end of one field into the start of the next would pass as a substring of a string that exists nowhere in the source. Path resolution and the tracked-destination refusal are the translation map's own functions rather than copies - both files sit beside the same material under the same rules, and a second copy is a second set of edge cases to get wrong. `CutMissingError` is a ValueError so a caller catching the import's refusal type stops on it too, and it carries the whole shortfall, because naming one identifier per failed run is 55 runs. Measured by five breaks, each restored: the substring check removed, the haystacks joined, the missing cut returning `{}` instead of raising, the empty slot read as absent, and the unknown slot ignored.
- [x] 8.2 Split a fused source entry by reading that map only, never by parsing its prose, each row recording the source identifier, and verify a test asserts no single row carries both a camera position and an act `backend/mining.py`. `split_fused_entry` takes the entry and its cut and emits one row per slot the cut names, wording byte for byte, each row carrying the identifier the source gave the entry - read by `identifier_for` from the extractor's own `IDENTIFIER_KEYS` rather than a fourth spelling, because three walks disagreeing about what an entry is called is how a row gets attributed to nothing. The substring rule is inherited from 8.1 and re-run here rather than trusted: the split refuses a typed fragment itself. The test that matters is not the row count - a split handing every row the WHOLE fused string produces three rows and passes any count - so each row's wording is asserted to hold none of the other slots' text. A second test feeds an entry whose prose has two obvious commas and a map that cuts on only one of them, and asserts two rows: a parser returns three there, and the extra one reads like a row somebody wrote. `split_fused_entries` collects the shortfall across the whole upload before any row is built. **The break for that passed the first time it was tried**: with the up-front check deleted, `cut_for` inside the loop still raises `CutMissingError`, so a test reading only the exception type is green on a split that stops at the first gap. The test now runs TWO missing entries and asserts BOTH names, which is the only observable difference between collecting and stopping - see [[idevgen-test-that-cannot-fail]]. Measured by five breaks, each restored: every row handed the whole entry, an omitted slot emitted as an empty row, the identifier dropped, the shortfall raised late, and the split parsing on the comma instead of reading the map
- [x] 8.3 Produce no row for a part the entry does not carry, and verify a test over a camera-and-room-only fixture asserts no act row is written The same function, and a separate property from 8.2: an omitted slot produces NO ROW, not a row with an empty wording. An empty row would be drawn, composed and judged like any other and its verdict would be about a blank. The fixture is a camera-and-room entry with nothing describing her body, and the test asserts the slots that came back, that no act row is among them, and that every wording is non-empty. Measured by the break that emits a row for every slot regardless, restored
- [x] 8.4 Resolve every template placeholder before storage or skip and report the entry, and verify a test rejects a seed row containing an unresolved placeholder **This one was already broken in shipped data.** Three imported rooms were on disk carrying `{pose}` - `medical-rehab-therapy`, `medical-exam-table-bend` and `sm-urban-petplay-art-gallery-opening` - sending the braces to the sampler verbatim on every draw. `PLACEHOLDER_PATTERN` requires a NAME inside the braces, because `guidance` is always written empty-dict-included and a pattern matching a bare `{}` refuses 22 tests' worth of ordinary rooms - measured, that break failed 22. `resolve_placeholders` fills a hole ONLY from a field of the entry with that exact name holding a non-empty string, and leaves the rest standing: a hole quietly deleted is a sentence with a piece missing that still reads as English, which enters the catalogue and gets judged. Run AFTER translation, because the map is keyed on the source string hole and all, and filling first hands the map a string it has no entry for. The check is over the WHOLE ROW and not over `place`, and the test that forces that is a fixture whose hole is in the LABEL with a clean theme - **the place-only break passed until that entry was added**, since all three real ones are in `place`, which is exactly why `place` is not the check. A skipped entry takes its stale row with it: the identifier is already in `seen_incoming_ids`, so last import's copy is neither merged nor kept as an orphan. Otherwise the report says the room was not written while the seed file goes on shooting it. The three unresolvable rows were dropped from the local seeds, which is what a re-import now does to them. `tests/test_placeholders.py` scans every seed the data directory actually holds rather than a named list: the imported seeds are untracked, so a named list would pass on a fresh clone for the same reason it would miss the defect here, and a clone with nothing imported scans the tracked seeds and correctly passes. Measured by five breaks, each restored: the hole deleted instead of reported, the skip removed, `place` checked alone, the stale row kept, and the empty brace pair counted
- [x] 8.5 Add the `pov` manner by inheriting the baseline manner and changing only its identity, with no instruction prose of its own, and verify a test asserts its brief and line are empty `POV` in `frontend/src/kinds.js`, spread from `BASE_MANNERS[0]` - `directed` - with only `key`, `label` and `blurb` changed. Directed's brief and line are both the empty string, so this ships no prose: `pov` IS directed's instruction until 8.19 measures whether a block is worth writing, and what the mined rows gain today is a namespace where their verdicts belong to them. The test does NOT stop at the two empty-string assertions the task names: it compares `pov` to `directed` FIELD BY FIELD and asserts the key sets match, because a later edit handing `pov` a defect plan or a technique field would pass a brief-and-line check while making it a different manner. Measured by four breaks, each restored: an instruction block written into `line` (2 fail), a `defects` key directed does not have (1 fail), the spread taken from `candid` instead of the baseline (2 fail), and `POV` left out of the export (3 fail). **The first version of this put `pov` in `MANNERS` and broke four tests**, correctly: `main.py` refuses session creation for a manner whose camera catalogue is empty, and `test_the_candid_catalogue_is_what_was_measured` asserts no manner ships unplanned. A `pov` in the shoot picker today is a button that always 422s. So the list is split - `MANNERS` is what the operator can start a session in and `pov` is not in it until 8.14 brings its rows, `ALL_MANNERS` is every manner that exists, and `MANNER` is built from the latter so a shot recorded under `pov` still renders a name instead of `undefined`. The guard was not weakened to let the change through; the change was wrong
- [x] 8.6 Declare the manner per source family - the participant-camera families to `pov`, the unattended-camera family to the existing manner that describes one - and verify a test asserts every mined row carries the manner its family declares `SOURCE_FAMILY_MANNERS` in `backend/mining.py`, four keys: the three participant families to `pov` and `fisheye_pov` to `candid`. Declared per FAMILY and read once per ENTRY, which is the property the test pins - a manner decided per row lets one entry's camera be judged in one manner and the act beside it in another, which is two matrices describing one photograph. Family names are NORMALISED before lookup (`Rear-Entry POV`, `rear entry pov` and `rear_entry_pov` are one family): the source writes its families as labels, and a declaration keyed on one capitalisation is a declaration that misses the same family written the other way. The entry names its own family under `source_family`, `family` or `category` - the extractor's `IDENTIFIER_KEYS` shape and for its reason, one fact spelt several ways - with an explicit `source_family=` argument on `split_fused_entry` for the caller that knows better, the same shape `identifier=` already has. **Nothing is defaulted.** A family nothing declares refuses the entry by name, and so does an entry naming no family at all: `directed` is the one answer the design rules out, because its instruction says somebody is photographing her and a dead verdict inside it would mean the mismatch rather than the row. Both halves raise `FamilyUndeclaredError`, one class because the operator's next move is the same either way, and it collects the whole shortfall like `CutMissingError` - a library of 55 is not one identifier per re-run. The test asserts BOTH names, which is the only observable difference between collecting and stopping, since the loop's own lookup raises either way (the trap 8.2 fell into). The `directed` assertion is over the whole declaration's values rather than the three family names, so a family added later is covered on the day it is added. Measured by five breaks, each restored: the manner hard-wired to `pov` (2 fail), an unknown family defaulting to `directed` (1), the shortfall raised inside the loop (1), the manner written onto the camera row alone (1), and the normalisation removed (9). `python -m pytest` at 667
- [x] 8.7 Derive each mined act's requirement from its wording by the reading the catalogue already uses, floored per family, setting the requirement where the reading is uncertain, and verify a test asserts an act whose wording omits the second body still carries it from the family floor `needs_for_act` in `backend/mining.py`, writing `needs` in the act catalogue's own vocabulary - `''` for pure geometry, `'him'` for an act with a second person in it. The wording reading is `derive_multi_body` IMPORTED from the importer and not respelled: the rooms and the mined acts answering the same words two ways is exactly the drift this import exists to avoid. **The family floor is read off the manner declaration and is not a second list.** A family is mined into `pov` precisely because its camera is held by a participant, and a participant is a body in the photograph that is not hers - so `second_body_families()` is `SOURCE_FAMILY_MANNERS` filtered on `pov`, and a family added tomorrow is floored the day it is declared. Two lists saying one thing is two calculations of one fact, which is the bug this repo has now found several times; the break that hardcodes the set fails two tests. The uncertain reading is its own vocabulary - `someone`, `somebody`, `another`, `partner`, `his`, `they`, `them`, `their` - the words `derive_multi_body` deliberately leaves out because over a ROOM's prose they are as likely to be her. Over an ACT they are not, and an uncertain reading SETS the requirement: set in error it only narrows the pool of a run that never switched the second body on, omitted in error it deals a two-body act into a single-body photograph, which this repo has already shot. `needs` lands on the ACT ROW ONLY; a camera or a room carrying `him` would narrow a pool for a requirement nothing about it asks for. The floored-family test asserts through the SPLIT and not only through the reading, because a derivation that is right in isolation and never reaches the row is the same as no derivation. Measured by six breaks, each restored: the floor removed (1 fail), the floor applied to every family (3), the catalogue reading swapped for a local word check (1), the uncertain words ignored (1), `needs` written onto every row (1), and the floor listed separately (2). **The uncertain-word pattern shipped with a literal backspace byte in it** and the test caught it, red on the first run: `re.escape` never sees the boundary and the pattern matched nothing. That is [[idevgen-invisible-byte-lesson]] recurring for the third time - the writer script built the escape from a non-raw triple-quoted string. The repair built the backslash with `chr(92)` and asserted `chr(8)` was gone from the whole file, because there were TWO of them and replacing the first left the second standing. `python -m pytest` at 673
- [x] 8.8 Report and refuse to write a mined act whose requirement cannot be determined, and verify a test asserts nothing is written and the entry is named `ActRequirementUndeterminedError`, raised by `needs_for_act` and collected by `split_fused_entries`. **The reachable case is narrower than it sounds and finding it was the task**: 8.6 already refuses an undeclared family and the cut map already refuses an empty or whitespace slot, so the only wording that arrives unreadable is one with no WORD in it - `", "` typed into the map where the clause was meant to go, which is a substring of the entry and passes every check the map makes. `_HAS_A_WORD` is `[A-Za-z]{2,}`, a letters check and not a grammar, with the upgrade path named in a `ponytail:` comment. The check runs BEFORE the family floor and not after, which is the break that matters: three of the four families floor every act they carry, so a check written after the floor never runs for the families whose acts actually carry the second body. The floor says what a family always needs, not what a row needs, and a row with no words in it has no reading to floor. In the batch the shortfall is collected in a `try` inside the loop rather than raised where it is found - the loop is the only place the act wording exists - and NOTHING comes back when anything is short, not even the readable entries' rows. The rows of the good entries are built in memory and dropped, which is what "reported and not written" means here: a report that skipped the bad entry and returned the rest would leave a source entry mined into two rows of three with nothing saying so. Both the two-name assertion and the whole-upload one are needed - stopping at the first raises the same exception type, and a test reading only the type is green on a report that hands the operator one identifier per re-run. Measured by four breaks, each restored: the refusal removed (3 fail), the check moved below the floor (1), the shortfall re-raised inside the loop (1), and the bad entry skipped with the rest returned (2). `python -m pytest` at 676
- [x] 8.9 Keep a mined act carrying the second-body requirement out of a run that has not switched it on, and verify a test composes a single-body run and asserts no such act is drawn **No new gate was written and that is the finding.** `_draw_n_trio_shots` already narrows the act pool to what the run provides - `runs_with` gains `him` only under `with_him` - so a mined act needs nothing but the same word in its `needs` field to be covered by it. Writing a second gate for mined rows would be a second calculation of one fact, the bug this phase has now caught three times. So the task became a test that the two modules AGREE, and it is written end to end: the act is not typed into the test, it comes out of `split_fused_entry` carrying whatever the mining derived, and goes into `compose-run` as a candidate. Its wording never names a second body - the family floor is what put the requirement on it, which is the case 8.7 exists for - and the run without `with_him` draws only the plain act. The `with_him=True` half is the one that catches a mining that invents its own word: the filter keeps acts whose `needs` is in the provided set, so an act declaring `second_body` is absent from the pool in BOTH directions, and a test asserting only the single-body run reads that as a pass. Measured by two breaks, each restored: `NEEDS_SECOND_BODY` renamed to `second_body`, and the run-level filter deleted from `main.py`. Both fail the one test. `python -m pytest` at 677
- [x] 8.10 Store recorded combinations as row keys with no row text, in a tracked file, and verify a test asserts the file carries no prose and that a combination survives its rows being absent `data/mined-combinations-seed.json`, keyed by source identifier, three slots holding ROW KEYS - the same split the room verdicts live under and for the same reason: our own record in one file with somebody else's text loses the record to a licensing decision. `validate_combination` refuses a value that is not key-shaped, which is the half that makes the storage rule real: a combination built out of WORDS goes on reproducing the old photograph the day one of its rows is reworded, and nothing says the row moved. **A reference and not a foreign key.** The loader looks up nothing: a combination whose rows are absent comes back whole, because dropping it would silently delete the only record of what the source entry was on the day somebody retired one row. Reporting it broken is 8.13's job, and it cannot report what the loader threw away. An absent file reads as no combinations, the state of a fresh checkout. The file ships EMPTY - there are no mined rows to name until 8.14 - which is why the prose scan over it is explicitly not the falsifiable half: an empty file passes any scan. The test that can fail is the fixture with a clause where the key belongs. Named without the word `room` in it on purpose: `is_room_seed_file` reads any `*-seed.json` carrying `room`, and a file it registered would be offered through the picker as a library of rooms. Added to `.gitignore`'s allow-list beside the other tracked seeds. Measured by four breaks, each restored: prose accepted, the unknown slot ignored, the loader dropping what it cannot find, and an empty combination stored. `python -m pytest` at 681
- [x] 8.11 Compose a recorded combination from the app without hand assembly, and verify a test asserts it is never dealt by a session's draw `POST /api/sessions/{sid}/compose-combination`, payload of ONE field - the source identifier. A payload carrying the rows would be hand assembly with a lookup in front of it, and it would compose whatever was typed rather than what the entry was. The keys are resolved to catalogue rows at compose time, so a reworded row composes its new words and a row that is gone refuses. **Two refusals the route is mostly made of.** The ROOM is the session's look and this route does not write it - `SessionPatch` cannot reach the look on purpose (7.2) - so a session filled from another room is refused rather than having the entry's camera and act dropped into somebody else's place. And a row whose MANNER is not the session's is refused, because a cell is keyed on the manner and the same words measured elsewhere are another measurement. **No framing, and that is the source's shape.** Its library has no crop field at all - session 391 measured that at 6 of 6 knee-up crops - so a combination names three parts and not four; `_sentences` drops the empty piece and the line carries what the entry carried. Inventing a framing would compose a photograph the entry never was. **The never-dealt test was a test that cannot fail on its first writing.** It asserted the drawn shot was not the mined pair, and the break that feeds the whole combination store into the candidate list PASSED it: the mined trio had no verified cell, so the cell gate was hiding the fact that nothing else was. Rewritten to assert on the POOL SIZE - both trios verified, only the session's own offered, a run asking for two distinct trios refused - which is the one observable difference between a draw that reads the store and one that does not. See [[idevgen-test-that-cannot-fail]]. Measured by five breaks, each restored: the room check removed, the manner check removed, a framing invented, the missing row composed from what was left, and the draw reading the store. `python -m pytest` at 687, frontend build green
- [x] 8.12 Record the combination each mined source entry was split into, and verify a test composes one recorded combination and asserts it reproduces the source entry's photograph `row_key`, `combination_for`, `record_combinations` and `save_mined_combinations` in `backend/mining.py`. **The key is DERIVED at the split** - `mined-<identifier>-<slot>` - and not handed out at storage time: the combination has to name the rows it was split into, so a key that only existed once the rows were written would leave the split with nothing to record. Deterministic, so re-mining the same library twice names the same rows, which is what makes 8.15's duplicate report possible at all. The record is grouped from the ROWS the split produced, never recomputed from the entries - a second walk deciding what a combination holds is a second calculation of one fact, and the day it disagrees the record names rows the split never wrote. A part the entry never carried is absent from its combination too, which is what stops 8.13 reporting every camera-and-room entry broken forever. The reproduction test is end to end and types no key: the entry is cut, the rows carry their derived keys, the record is written to the file the app reads, and the route is asked for the entry BY NAME. The three source clauses are then asserted byte for byte inside the composed prompt - camera and act from the rows, room from the look the session was filled with. A reproduction that composed the right rows into the wrong words is a photograph nobody measured wearing the identifier of one somebody did. Two guards the task did not ask for and the corpus needs: a **collision** between two identifiers that normalise to one key stops the split, because stored it merges two photographs into one row and one combination overwrites the other with nothing saying a photograph was lost; and the writer **replaces** the file rather than merging, so a re-cut entry leaves no stale record. **The merge break passed every test in the file** on its first run - the docstring claimed replace-not-merge and nothing checked it, which is [[idevgen-test-that-cannot-fail]] in its quietest form: a claim in prose is not a check. A test was added and the break then failed. Measured by five breaks, each restored: the key ignoring the slot (3 fail), the combination naming all three slots regardless (1), the writer merging (1, after the new test), the collision guard removed (1), and the record grouped under one name (2). `python -m pytest` at 693
- [x] 8.13 Report a recorded combination as broken when one of its rows is retired or reworded, and verify a test asserts it is not composed from the remaining rows `wording_digest` and `combination_breakage` in `backend/mining.py`, spent by the compose route. **A key alone cannot see a rewording**, which is what this task turned on: 8.10 stores references and no prose, so a stored key resolves to whatever that row says today and the route was composing it - its own docstring said so, that a reworded row composes its new words, which is the line the spec contradicts. So each slot now holds a key AND a 16-character digest of the wording it was split from. A digest is not prose: it reproduces nothing and reads as nothing, and it is exactly enough to answer the one question asked. The wording is trimmed before hashing because every writer of a wording trims it - the catalogue PATCH strips, the import strips - and a digest that moved on a trailing space would report a row broken that nobody touched; the break that drops the strip fails the same-words test. **Retired and absent are reported apart**, which is why the route's lookup no longer filters on `retired_at`: both refuse, but a retired row is on disk and the next move is to restore it, while an absent one needs the import re-run, and one message for both leaves the operator guessing. The break that puts the filter back fails one test on exactly that. The whole shortfall is reported at once rather than at the first fault, the rule the cut map and the family declaration already answer under. **The room slot is checked for identity and not for wording, on purpose.** It is not a component - it cannot be retired and the catalogue screen cannot reword it - and its text in a session is the look, which the operator owns and may legitimately edit; a look edit reported as a broken row would refuse a photograph nothing happened to. So `combination_breakage` judges only the slots the caller hands it, and a slot missing from that map is not judged rather than reported absent. The break that removes that distinction fails EIGHT tests, because every combination naming a room is then broken forever. The upgrade is named in a `ponytail:` comment: the day mined room rows are imported into a room library they resolve like the other two and get the same check. Measured by six breaks, each restored: the breakage call removed from the route (4 fail), the retired filter restored (1), the digest never compared (2), the report stopped at the first fault (1), the digest untrimmed (1), and an unresolved slot judged anyway (8). `python -m pytest` at 697
- [x] 8.14 Store mined camera and act rows unverified with a judge label on each camera, and verify a test asserts the verdict and the presence of the label `component_rows` in `backend/mining.py`, and the store is `/api/components/import` - the catalogue's own. **No second writer**: a mined row is a candidate row like any other, and a second insert path would be a second set of rules about what a component is, which is the bug shape this phase has now caught four times. **Unverified is not a fourth word.** The verdict vocabulary is three, bound to `db.cell_state`, and a mined row with no cell behind it reads `unknown` at `judged=0` through `/api/components` without anything being written to say so. The test asserts it THROUGH the app rather than on the dict, because what matters is that nothing on the storage path made the row look measured. `family`, `faces` and `cameras` are left EMPTY for the same reason and it is a statement, not a gap: each is a reading somebody took, `fitCameras` walks `cameras` to plan a shoot, and a guess there arrives downstream as a finding nobody has. The break that fills one fails its test. **The judge label is curated and cannot be derived.** It is the sentence the blind judge is asked to confirm; this module parses no prose, and a label cut down from the wording is the wording again, which the schema's own CHECK refuses. So `mined-judge-labels.json` is read beside the source material, UNTRACKED and resolved by the cut map's own resolver, with `save_mined_labels` refusing a tracked destination through `_is_tracked_location` - borrowed and not respelled. The rule the file follows is this change's existing one: prose ABOUT a source entry stays out of git, references do not, which is why `mined-combinations-seed.json` is tracked and this is not. `JudgeLabelMissingError` reports the whole shortfall and reports its two halves APART - a row with no label needs one written, a row whose label repeats its wording has one and it is unusable - because the repairs are different. Nothing is stored when anything is short: a store that inserted the labelled rows would leave one source entry mined into a camera with no act and nothing saying so. A ROOM row is dropped rather than refused, since the catalogue's three slots are the three dimensions of a cell and a room reaches a photograph as the session's look. One test was DELETED for being unable to fail: it asserted no cell exists behind a stored row, which no break of this code could make false - `/api/components/import` writes no cells and never did. See [[idevgen-test-that-cannot-fail]]. Measured by seven breaks, each restored: `family` filled in (1 fail), the room row stored (4), an unlabelled row let through (1), a label repeating the wording let through (1), `needs` dropped (1), the manner hard-wired to `directed` (1), and the tracked destination allowed (1). `python -m pytest` at 702
- [x] 8.15 Report a mined row whose wording duplicates an existing catalogue row and create no second row, and verify a test asserts the duplicate is reported and the row count is unchanged **No new gate, and that is the finding for the second time this phase.** `/api/components/import` was already idempotent on `(slot, manner, concept_key OR wording)` and already skipped rather than updated - the half that was missing is that it only COUNTED. A count says something was already there and nothing about what, so the task was to NAME them: `duplicates` now carries the incoming key, the existing key, and which of the two matched. **`matched_on` is the report.** A KEY match is the same row arriving twice, which re-mining a library does every time and is ordinary. A WORDING match under another key is two rows saying one thing, and that is the finding worth acting on: stored, the two are judged apart and one photograph's evidence is split across two cells, so the operator's next move is to point the source entry at the existing key. The break that always reports `key` fails the test that separates them. **The scope stays inside one manner** and the test that pins it is the third one: a cell is keyed on the manner, so the same clause under `directed` is another measurement and not a duplicate of the `pov` row. A duplicate report that ignored the manner would refuse to store a mined camera because a directed row happens to use the same words, and the pov matrix would carry a hole where a measurable row belongs. The break that drops the manner from the scope fails exactly that one. No prose crosses the wire: the report names KEYS, and the existing row's wording is never returned. Measured by five breaks, each restored: the duplicates never reported (2 fail), `matched_on` always `key` (1), the wording half of the match dropped (1), the manner dropped from the scope (1), and an existing row overwritten instead of skipped (2). `python -m pytest` at 705
- [x] 8.16 Record the camera concepts the mining introduces that the catalogue did not hold, and verify the record names the feet-first low POV and the overhead-over-kneeling-subject concepts `NEW_CAMERA_CONCEPTS` in `backend/mining.py`, two entries. It is a finding about OUR catalogue written in our own words and read by a test rather than by the app, so it is a tuple and not a seed file: a JSON file would need a loader, a validator and a place in the registry checks to say what a tuple says. **Each entry names the rows it is NEAREST to, and that is what makes the record accountable.** "The catalogue has nothing like this" is unfalsifiable prose. Naming `worms-eye`, `ground-level`, `floor-low-angle` and `low-angle` and saying they answer how HIGH the lens is and never where around her it stands is checkable, and the test asserts the half a test can check: those rows exist. The break that invents one fails it. The two concepts and why each is new: **feet-first low POV** is on the AXIS of her body at its foot end, and the catalogue's low cameras are all silent about the axis - the one row that names the effect, `cand-foreshortening`, is a treatment to look for with no camera standing anywhere. **Overhead over a kneeling subject** is a participant's own eyeline at standing height onto a body kneeling below it, and every overhead the catalogue carries (`top-down`, `overhead-direct`, `high-angle`, `overhead-term`) looks down at HER as the whole subject; the catalogue has no camera that says where a second body is. Both follow from the one thing the mined families share - the camera is held by a participant. The catalogue is read off the TRACKED seeds and not out of the database, because the database on any machine holds whatever that operator imported and the claim is about what this repository ships. The test enforces the record in both directions: the keys must be absent from the shipped seeds today, so the day one of these is measured and added under its key the test fails and the entry has to come out - a record of what is missing that names a row somebody has since written sends the next reader to import it twice. Measured by five breaks, each restored: a near miss no row carries (1 fail), a concept key the catalogue already holds (2), a concept dropped from the record (1), an empty judge label (1), and no near misses named (1). `python -m pytest` at 708
- [x] 8.17 Keep unverified mined rows out of the strict composer's draw, and verify a test asserts an unverified mined camera is never drawn on the strict path **No new gate, for the third time this phase.** `_trio_pool` in `strict` is `judged >= 10 AND arrived*10 >= judged*8` over the cell table, which is `db.cell_state`'s definition in SQL, and a mined row is out of it by the rule that was already there. A gate keyed on "is this row mined" would be a second calculation of what drawable means, and the two would disagree the first time either was edited - so the task became a test that the two AGREE, written end to end from the split. **The first two tests could not see the break that matters**, and finding that was the work. A trio nobody has judged has NO ROW, so a strict predicate loosened all the way to `AND 1=1` still excludes it and both tests stayed green. The case is the one 8.18 creates: a mined trio part way through judging HAS a cell and is `unknown` until the tenth photograph, and a pool that asked the table for a row rather than for a VERIFIED row would draw it at a sample of four while reading as measured. The third test writes that cell, and both loosened predicates then fail. See [[idevgen-test-that-cannot-fail]]. Every refusal is paired with its opposite in the same test - the same call in `exploratory` queues the shot, and the same cell at the threshold is drawn - so what is being asserted is the MODE and the sample size rather than anything being wrong with a mined row. The pass-over test asserts on the POOL SIZE rather than on what came back, the shape 8.11 had to be rewritten into: a run asking for two distinct trios out of a pool of one is refused, which is the observable difference between a pool that holds the mined trio and one that does not. Measured by three breaks, each restored: the strict predicate loosened to any cell (1 fail), the ratio dropped from it (1), and strict widened to exploratory (3). `python -m pytest` at 711
- [ ] 8.18 Judge the new camera concepts under the project's existing blind judging protocol and record their verdicts, and verify the recorded sample size matches the protocol's minimum
- [ ] 8.19 Shoot the recorded combinations against their mined parts drawn separately, and verify the result is recorded as the evidence for whether a `pov` instruction block is worth writing
- [x] 8.20 Declare where the fused library's entries land now that the split exists, and verify a test asserts the room importer is refused and the mining path is not. **Found by setting the mining run up, not by reading the plan.** 4.1 declared `perspective_scenes` with NO destination on `not_split_yet`, which was true when nothing could cut it - and it means the manifest refuses the file outright, so the corpus cannot be mined at all while that stands. An empty destination list said "the room importer must not write this" by claiming the material reaches nowhere, and that stopped being true the day 8.2 shipped. So the fused library now names `perspective-scenes-rooms-seed.json` like its siblings, and the rule that keeps the room importer out moved onto the KIND: `declare_source_file` takes `writes`, the kind of material the CALLER can write, and refuses a mismatch with `REASON_WRONG_IMPORTER`. It is the stricter form as well as the honest one - an empty list refused both importers, this refuses the room importer while letting the mining path through. Only the ROOM part of a cut entry lands there; the camera and act rows are not named because a destination is a FILE this app reads at runtime and a mined component goes through the catalogue's own import route. **`writes` is required with no default and that is the whole of its safety.** A default makes it optional, and an importer that forgot to say would take whatever the default happened to be - one importer silently claiming another's material. The break that gives it a default passed every test until a test was written for the signature itself, which is the shape of [[idevgen-test-that-cannot-fail]] at the level of an argument. It only ever NARROWS - it cannot declare a library, add a destination or lift the not-adopted refusal, tried at every kind - so it is not one of the bypass keywords `test_no_caller_argument_declares_an_undeclared_file` refuses. `REASON_NOT_SPLIT` is deleted rather than left standing: a reason nothing raises is a reason that reads as a live rule. Measured by five breaks, each restored: the kind check removed (1 fail), `writes` given a default (1, after the signature test), the room importer claiming the fused kind (33), the fused destination removed (2), and the fused library declared as rooms (1). `python -m pytest` at 714
- [x] 8.21 Declare each fused entry's family from a curated file, and verify a test asserts an undeclared family is refused at load and that a declared one reaches the split. **The corpus does not carry this fact, which is what the setup found.** 8.6 reads the family from `source_family`, `family` or `category`, and the real library names a `category` that is the ROOM it is set in - `special_visual`, `classroom`, `bedroom` - so `family_for` meets a word nothing declares and refuses all 55 entries. The family is the operator's to state, which is what `split_fused_entry(source_family=)` already existed for; what was missing was somewhere to state it. `mined-families.json`, untracked beside the cut map and the labels, one line per source entry. The VALUES are our own vocabulary, and that is what lets the loader check them: a family `SOURCE_FAMILY_MANNERS` does not declare is refused at LOAD, while the whole file is read, rather than at the split - a typo caught at the split stops the run with the entries before it already cut, and the operator fixes one line per re-run. The whole shortfall is named for the same reason, and the two undeclared entries in the test are what makes that observable. The second test is end to end and is the one that matters: a loader that validated a family and handed back something the split did not use would be a check with no effect, so the entry is given the corpus's own `category` word, asserted to refuse on its own, and then split with the declared family and asserted to land in `pov`. Measured by four breaks, each restored: the family checked at the split only, the shortfall raised at the first bad line, the family not normalised, and the tracked destination allowed. One test fails on each. `python -m pytest` at 716

## 9. The writer field set

- [ ] 9.1 Add `accessories`, `marks`, `style`, `props` and `story` to the writer's field list and to the joiner's headings in the same edit, with the all-fields statement and the count updated to twelve, and verify the test that binds the two lists passes
- [ ] 9.2 Update the JSON skeleton's own heading and the regex that reads it - `THE SIX FIELDS` becomes the eleven the skeleton carries once `technique` is left out - in `frontend/src/kinds.js` and `tests/test_enhance.py` in the same edit, and verify the test still fails when a field is added to one list and not the other
- [ ] 9.3 Keep `hair` and `makeup` out of the field list, the key header and the joiner's headings while the session's look defines them, and verify a test asserts neither name appears in the writer's instruction and that a composed line answers hair once
- [ ] 9.4 Write the field descriptions so `style` carries lens, medium and treatment only and `marks` carries her skin only, with neither carrying a camera position or a framing, and verify a measurement run reports a position written into `style` as a fault
- [ ] 9.5 Keep `technique` out of the key header and out of the JSON skeleton, as the control has it, so the lens is answered once, and verify its arrival stays at the control's rate
- [ ] 9.6 Re-run the writer measurement against the shipped control at the documented protocol, and verify arrival, survival and the arrival of the pre-existing fields all match the measured arm
- [ ] 9.7 Add `with_tattoo`, `with_pet` and `with_liquids` as run-level switches in the same shape as `with_him` and `with_furniture`, defaulting off, and verify a test asserts each name appears nowhere in the writer's instruction when its switch is off
- [ ] 9.8 Require the run to supply the subject when such a switch is on, refusing the run before the shoot is written when it is not, and verify a test asserts the refusal names the field
- [ ] 9.9 Carry a supplied subject verbatim across the shoot in the photographs that do not change it, and verify a test asserts the supplied words are repeated unchanged
- [ ] 9.10 Add the three switches and their subject inputs to the compose panel in `SessionView.jsx`, in the same shape as the `with_him` and `with_furniture` checkboxes it already carries, and verify `npm --prefix frontend run build` succeeds and a frontend test asserts a switched-on field with an empty subject cannot be sent
- [ ] 9.11 Add the test that no line describes a switched-off field's subject, and verify it fails against a fixture line carrying an invented tattoo before it passes

## 10. Close-out

- [ ] 10.1 Run `python -m pytest` and `npm --prefix frontend run build` and `npm --prefix frontend test`, and verify all three are green
- [ ] 10.2 Confirm no tracked file contains a machine path, a real person's name or a non-English character, and verify `tests/test_no_personal_data.py` and the non-English test pass over the new seeds, the translation map and the scripts
- [ ] 10.3 Update README.md and the matching `docs/` pages for the room picker, the registry, the translation map and any shipped writer field, and verify each documented default matches the code
- [ ] 10.4 Rewrite the `use_look` tooltip in `ModelDetail.jsx`, which still warns that a look past ~85 composed words costs the position and framing, to say what session 394 measured and what the word budget now does, and verify the number it quotes matches the budget 7.9 sets

