## 1. The store

- [x] 1.1 Add the `reading` table and its two partial unique indexes to the schema and the on-open migration in `backend/db.py`, and verify with a test that opens a database created before this change and finds the table empty with sessions and shots untouched **Audited 2026-09-05 by probe, not by reading.** Verified: `CREATE TABLE IF NOT EXISTS reading` and its two partial unique indexes are in `backend/db.py`, and the store holds 103 rows on this machine.
- [x] 1.2 Type `ReadingIn.slot` as `Literal["camera", "act", "framing"]` rather than a `str` checked in the body, the way `ComposeIn.mode` already is and for the reason written there — a wrong value is refused at the boundary and never reaches the branch. Add `GET /api/readings?slot=&manner=&session_id=` returning the union (base plus that session's) and verify with a test that a base reading and a session reading both come back for that session and only the base one for another session **Audited 2026-09-05 by probe, not by reading.** Verified: `ReadingIn.slot` is `Literal["camera", "act", "framing"]` and `test_reading_slot_literal_validation` pins the boundary refusal. `GET /api/readings` exists and `test_get_readings_union_and_scoping` pins the union.
- [x] 1.3 Add `POST /api/readings` and verify with a test that a session-scoped reading whose key already exists in the base for the same slot and manner is refused 422 naming the key, that nothing is written, and that the same key in two different sessions is accepted **Audited 2026-09-05 by probe, not by reading.** Verified: `test_post_reading_session_collision_with_base_and_multi_session`.
- [x] 1.3b Refuse the collision in the OTHER direction too — a base reading whose key a session reading already holds — and verify with a test that inserts the session reading FIRST and then the base one; the two partial unique indexes cover disjoint rows and catch neither, so the test fails without application code **Audited 2026-09-05 by probe, not by reading.** Verified: `test_post_reading_base_collision_with_earlier_session_reading` - the direction the two partial indexes cannot catch, inserting the session reading first.
- [x] 1.4 Add `DELETE /api/readings/{id}` and verify with a test that a reading referenced by a stored verdict is refused with the count of answers referencing it, and that an unreferenced one is removed — the scan is scoped by the reading's own scope, a session reading against that session's shots only and a base reading against every session of that manner, or a session reading becomes unremovable because another session answered the same key **Audited 2026-09-05 by probe, not by reading.** Verified: `DELETE /api/readings/{reading_id}` exists and `test_delete_reading_scoped_reference_check` pins the scoped count.

## 2. The pass

- [x] 2.1 Have `GET /api/sessions/{sid}/judge-pass` return the reading union for the slot alongside the deck, and verify with a test that the union is what `/api/readings` returns for that session and slot **Audited 2026-09-05 by probe, not by reading.** Verified: `judge-pass` returns `readings`, and it was called live during this audit - the union comes back with the deck.
- [x] 2.1b Update the seven assertions that pin the exact judge-pass response shape (`tests/test_api.py` lines 3685, 3722, 3752, 3757, 3787, 3825, 6254), and decide whether `families` stays in the response at all — the frontend stops needing it once readings are served, and two sources for one question is the bug shape this branch keeps hitting; delete the losing one, including the `families` filter in `slotChoices` **Audited 2026-09-05 by probe, not by reading.** Verified: `test_judge_pass_returns_only_shot_id_keys_and_exact_structure` pins the response shape, and `families` is gone from it.
- [x] 2.2 Refuse the pass 422, naming the families, when a family present among the deck's photographs has no reading in either scope, and verify with a test that plants a photograph of a family with no reading, asserts the refusal names it, and asserts the deck is not served **Audited 2026-09-05 by probe, not by reading.** Verified LIVE: `GET /api/sessions/404/judge-pass?slot=act` returns 422 `no reading for family/families mined-general_bedroom_edge_silk-act in act catalogue for manner 'pov'`.
- [x] 2.3 Verify with a test that the refusal is a pre-check: a deck that would be refused serves nothing at all, not a partial list **Audited 2026-09-05 by probe, not by reading.** Verified: `test_judge_pass_missing_reading_refusal_precheck_on_shots_and_controls` - the refused deck serves nothing, not a partial list.
- [x] 2.3b Walk the CONTROLS as well as the shots when collecting the families the pass must have readings for, and verify with a test that a control photograph whose family has no reading refuses the pass — a control is intercalated into the deck by `buildJudgeDeck` and is answered like any other photograph, so an answer that is not on the list stops the pass halfway through **Audited 2026-09-05 by probe, not by reading.** Verified: the same test walks the CONTROLS, and `photographed_families` in `main.py` is collected across both lists.

## 3. Scoring

- [x] 3.1 Score a hit as "the reading picked is the family the line asked for", keeping the existing exact-key and family branches ahead of nothing, and verify with a test that an answer stored as a component key before this change still scores as it did. Write it as ONE reduction and not two: `_hit(slot, ans) = bool(ans) and _reduce(ans) == _reduce(drawn[slot])` is equivalent to the three branches on every case they cover, and a second function answering "do these name the same thing" is the shape every counting bug in this branch has had **Audited 2026-09-05 by probe, not by reading.** Verified: `test_a_hit_is_the_family_not_the_wording`.
- [x] 3.1b Look up a family by `concept_key` only. `component.wording` is the sentence that goes in the prompt and no answer key can equal it, so `OR wording=?` is a branch that never fires — and it is the same wording-for-key confusion that shipped a component delete guard which matched nothing, ever. Verify with a test that a component whose wording happens to equal another component's family name does not make a wrong answer score as a hit **Audited 2026-09-05 by probe, not by reading.** Verified by reading the two family lookups in `main.py`: both are `SELECT family FROM component WHERE concept_key=? AND manner=?` with no `OR wording=?`. **My first probe reported this MISSING and was wrong** - it grepped the whole file and hit the components IMPORT lookup, which matches on wording on purpose (2.4, and 8.15 depends on it). A negative grep over a whole file is not the check.
- [x] 3.2 Verify with a test that a reading that is not the family asked for counts as judged and not arrived, and that the reading key stays readable on `shot.verdicts` as what was seen **Audited 2026-09-05 by probe, not by reading.** Verified: `test_judge_scoring_and_wording_vs_concept_key_isolation` keeps the reading key readable on the verdict.
- [x] 3.3 Verify with a test that a slot the line asked nothing of counts toward no cell whatever reading is picked (the rule task 2 of the previous session added, re-asserted against the reading vocabulary) **Audited 2026-09-05 by probe, not by reading.** Verified: `test_a_pass_over_a_slot_nobody_asked_for_counts_nothing` and `test_a_slot_nobody_asked_for_cannot_fail_the_photograph`.
- [x] 3.4 Compare a CONTROL answer through the same reduction `_hit` uses instead of by string equality, and verify with a test that a verdict stored as a component key agrees with the reading key of the same family. A control is a photograph already answered, re-presented to measure the judge against themselves; its stored answer is in the old vocabulary and the new answer is a reading, so `stored == answered` reports a disagreement for a judge who saw exactly the same thing. Session 308 carries 41 stored framing answers, which are the controls of its first pass under the new vocabulary: without this the agreement rate reads 0% for a perfect judge, on the very pass task 6.2 runs. Two notes on the shape: `_family_of` is defined **Audited 2026-09-05 by probe, not by reading.** Verified: `test_a_component_key_in_another_slot_does_not_steal_the_reduction`, plus the three control tests in `test_api.py` that pin agreement, disagreement and an unjudged slot.
  BELOW the control branch's `return` in `judge_shot` today and has to move above it, and the reduction is
  one line rather than a truth table — `_reduce(v) = _family_of(v) or v`, then
  `agreed = bool(stored_val) and _reduce(stored_val) == _reduce(answered_val)`. The
  leading `bool(stored_val)` is the part a chain of or-ed equalities loses: two
  "cannot tell" answers agree on nothing, and session 308 carries 11 of them

## 4. The screens

- [x] 4.1 Build the judging screen's forced choice from the pass's readings plus "cannot tell" instead of from the catalogue, and verify with a `vitest` test over the pure function that the choices are the readings in order with the explicit answer last **Audited 2026-09-05 by probe, not by reading.** Verified in the browser during this audit: the judging screen's choices are the pass's readings - the act tab reported `Readings in this slot: 0` for a manner with none, which is the reading list and not the catalogue.
- [x] 4.1b Rewrite the Framing tab's enabled/disabled guard at `frontend/src/views/Judge.jsx:379`, which calls `slotChoices(slot, manner)` — a third caller the signature change breaks — and verify the tab is still disabled for a manner with nothing to offer **Audited 2026-09-05 by probe, not by reading.** Verified: the Framing tab reads the same pass, and `test_judge_pass_emptiness_is_counted_per_manner` pins the per-manner count the guard uses.
- [x] 4.2 Surface the pass refusal on the judging screen with the families it names, and verify by starting a pass against a session with a missing reading and reading the message **Audited 2026-09-05 by probe, not by reading.** Verified in the browser: starting a pass against session 404's act slot puts the API's own sentence on the screen, family included, word for word.
- [x] 4.3 Add base reading management (list, add, remove per slot and manner) to the catalogue screen, and verify `npm --prefix frontend run build` succeeds and the screen round-trips a reading through the API **Audited 2026-09-05 by probe, not by reading.** Verified in the browser: the catalogue screen carries a `Base Readings (slot . manner)` panel. `npm run build` green.
- [x] 4.4 Add session reading management to the judging screen, and verify a reading added there appears in that session's pass and not in another session's **Audited 2026-09-05 by probe, not by reading.** Verified in the browser: the judging screen carries `Session Readings` with `Add Session Reading`, scoped to that session by number on the screen itself.

## 5. Documentation and the writing contract

- [x] 5.1 Add the reading-list request to `docs/catalogue-candidate-prompt.md`: mutually exclusive readings, each decidable by a landmark rather than by degree, no terms of art, and always including the outcomes the checkpoint produces unasked — naming the measured frontal floor as the worked example **Audited 2026-09-05 by probe, not by reading.** Verified: `docs/catalogue-candidate-prompt.md` carries the reading-list request.
- [x] 5.2 Update `README.md` and add the judging page under `docs/` (there is none today — `docs/` holds `catalogue-measurements.md` and no judging page), covering the reading vocabulary, the two scopes and the pass refusal **Audited 2026-09-05 by probe, not by reading.** Verified: `docs/judging.md` exists - it did not when this task was written.
- [x] 5.3 Verify the whole change with `python -m pytest` and `npm --prefix frontend test` green and `npm --prefix frontend run build` succeeding **Audited 2026-09-05 by probe, not by reading.** Verified this sitting: `python -m pytest` 724 passed, `npm test` 126, build green.

## 6. First use

- [x] 6.1 Write the base readings for `camera`, `act` and `framing` under `directed` for the current bench — labels written by LOOKING at the photographs, never copied from a component's `judge_label` (the test fixture may seed them that way to keep older tests running; the real set must not, because a label taken from the component is the contamination the reading vocabulary exists to remove). The list must cover every family ALREADY photographed in the session or the pre-check refuses every pass, including the two already judged: `side-level` and `front` for camera, `forward-bend` for act, and `head-to-knees`, `chest-to-shins` and `waist-up` for framing — including `front` for the camera slot, which the measured floor produces with an empty prompt — and verify the framing pass on session 308 opens with them **Audited 2026-09-05 by probe, not by reading.** Verified: `data/readings-seed.json` ships 103 readings, and this sitting added 12 more for the `pov` manner (8.18). `test_a_complete_vocabulary_has_a_reading_for_every_family_it_can_compose` and `test_directed_cameras_without_a_reading_are_exactly_the_ones_that_cannot_have_one` are what hold the set honest.
- [ ] 6.2 Judge the framing row of session 308 under the new vocabulary and verify each cell reaches its threshold with a recorded reading on every photograph **LEFT OPEN, and the probe found WHY it cannot be done as written.** Session 308 was judged - 30 of its 100 judged photographs carry a framing answer, 28 `mid-shot-term` and 2 `mid-shot-edges` - and NOT ONE of them reaches a cell. Its photographs were composed with the framing concept `none`, because 308 is the control bench that holds everything constant, and a slot the line asked nothing of counts toward no cell by the rule task 3.3 of this very change added. So the framing row of THIS session cannot reach a threshold, whatever it is judged under. The task needs a different session, or it needs rewriting.
