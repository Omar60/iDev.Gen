## 1. Browser-selected import and compatibility

- [ ] 1.1 Add server-owned selection_id/selection_revision manifests before raw uploads, with atomic cross-upload reservations, mutation invalidation, cross-selection refusal and idempotent consumed commit; enforce 20 files, 10 MiB per file, 50 MiB per selection across uploads and 24-hour expiry. Preserve private paths and conservative cleanup.
- [ ] 1.2 Adapt staging to canonical preview/commit, fingerprints, attestation, duplicate accounting and atomic accepted-set persistence; retain path API/CLI unchanged.
- [ ] 1.3 Default valid declared library identity exactly; add explicit compatibility targeting for missing/invalid declarations and supported non-envelope/auxiliary inputs. Detect exact source-ID/digest overlap under another key when the declared key is new and require an explicit target choice. Bind effective target to preview without renaming historical libraries.
- [ ] 1.4 Replace normal path/key entry with file selection and readable preview. Keep diagnostics and compatibility controls available. Test selection changes, late preview responses, stale commits, limits, expiry, same-library multi-file duplicates and no leaked stage paths.

## 2. Actionable translation readiness

- [ ] 2.1 Use selected translation-map content through existing preview/apply with a 10 MiB actual streamed HTTP-body limit enforced before JSON/Pydantic parsing (including missing/false Content-Length) and unchanged authorization/digest/fingerprint checks.
- [ ] 2.2 Build direct translation_map payloads from editable source-backed rows and route them through existing bulk preview/apply attestation; never use the direct revision mutation shortcut. Preserve canonical scalar/list contracts and diagnose conflicting duplicate source strings. Include explicit identity translations for already-English required strings without source fallback.
- [ ] 2.3 Reuse the configured assistant transport for optional translation proposals of at most twenty source entries per action. Refuse unsolicited/malformed fields and preserve canonical keys/list order; require review, preview and explicit apply with no proposal-time sidecar writes.
- [ ] 2.4 Present imported, needs translation, ready and needs source correction separately with direct next actions. Refresh readiness after apply; discard stale async proposals/previews.
- [ ] 2.5 Verify no-map/no-assistant manual completion, invalid source versus missing translation diagnostics, assistant failures, unauthorized proposals, stale library/map state and request bounds.

## 3. Authoring contract and atomic creation

- [ ] 3.1 Implement the complete closed authoring-v1 JSON schema from specs/session-plan/spec.md, including authoring.look_snapshot and wardrobe_progression nullable from this task, server-owned evidence rules and all variation-policy unions. Preserve every member through backend/frontend normalization and CAS. Default all dimensions to vary; fixed dimensions require user values and reject conflicting take overrides.
- [ ] 3.2 Preserve old/expert plans without authoring metadata and existing take-count semantics. Validate integer guided count 1-500 before allocating IDs, reject booleans/fractions/numeric strings, bound new/growing authoring-v1 plans and brief <= 2,000 characters; do not introduce a twenty-take session cap.
- [ ] 3.3 Add atomic guided session/initial-plan creation with character/workflow and ready rooms anchor validation, exact selected-resource membership and stable requested take IDs before remote calls. Test rollback with no orphan session.
- [ ] 3.4 Implement shared-state summary from existing authoritative look/initial_wardrobe and authorized scene descriptions. Empty additional constraints do not erase source descriptions.
- [ ] 3.5 Add optional one-time missing look/wardrobe suggestions through the configured assistant. Require acceptance through CAS before take consumption; preserve exact input/output, accepted values and user edits/origins. Support explicit empty constraints and manual/no-assistant decisions.
- [ ] 3.6 Extend generated continuity freeze to anchor/policy without rewriting generated/queued snapshots. Keep scoped wardrobe changes authoritative; progression is applied only through the explicit preset preview workflow.
- [ ] 3.7 Test round trips, mode changes without assistant configuration, conflicting policy values, stale suggestion acceptance and plans exceeding twenty takes.

## 4. Safe scene handling and structured assistant boundary

- [ ] 4.1 Restrict simple automatic anchors to ready rooms resources while retaining existing fused/manual/expert paths. Show advanced editing or structured-scene alternatives; do not auto-decompose fused prose.
- [ ] 4.2 Show complete authorized fused descriptions beside effective state and require existing user-approved adaptation or another resource for conflicts. Preserve immutable payloads and existing conflict gates.
- [ ] 4.3 Add/reuse a field-preserving backend.enhance helper with existing provider/authentication/timeouts and unchanged historical flattened callers. Use it for the requested proposal/authoring consumers without duplicating transport.
- [ ] 4.4 Bridge asynchronous calls into resource preparation validation/finalization with assistant provenance. Request only unlocked camera/framing/pose/expression; never persist assistant output as manual completion.
- [ ] 4.5 Test locked-field refusal, malformed/partial output, placeholders, shared-state protection and provenance replay. Demonstrate fused camera/pose conflicts and location changes embedded in pose without falsely treating structural validation as semantic approval.

## 5. Ordered automatic preparation and recovery

- [ ] 5.1 Build deterministic context from persisted brief, anchor/authorized scene state, shared values, policy, take ID/ordinal/total and at most five preceding finalized four-field summaries. Persist exact context and predecessor snapshot identities/revisions.
- [ ] 5.2 Implement sequential batches of at most twenty takes with per-take persistence and continuation for larger sessions. Reuse ready/generated snapshots and report completed/failed/remaining state on partial failure.
- [ ] 5.3 Add backend session-scoped exclusive authoring ownership shared by shared suggestions and take preparation, with operation identity, revision, lease/progress and fencing. Keep DB transactions outside remote calls.
- [ ] 5.4 Implement cancellation, expired-ownership recovery and stale-response rejection. Concurrent requests return active progress rather than duplicate calls; plan edits revoke ownership. Document possible remote retry after crash before persistence.
- [ ] 5.5 Add conservative downstream invalidation from the earliest edited/removed/reordered take for ungenerated automatic work, plus global authoring-input and existing wardrobe rules. Implement verified copy-forward in the plan CAS transaction for eligible unlinked ready snapshots, preserving original evidence and byte-identical prompts plus source/destination lineage. Recovery/submission must use the new-revision rows, never stale submission. Do not copy pending/invalidated/linked history to ready; manual work keeps input-based invalidation. Mode-only changes revoke review/cancel work while preserving completed provenance.
- [ ] 5.6 Flag normalized exact four-field duplicates against all finalized session takes without expanding writer history. Preserve deliberate repeats and require explicit review rather than deletion or unbounded retries.
- [ ] 5.7 Test two concurrent requests, cancellation during a call, lease expiry with a late owner, crash/resume after three takes, partial failure, edit/response races, downstream invalidation, distant duplicates and forty-take continuation using fake assistants. Exercise copy-forward recover/approve/submit, old-revision refusal, translation/context mismatch, duplicate-copy conflict and atomic rollback; linked history must not trigger duplicate generation.

## 6. Simple creation and review UI

- [ ] 6.1 Show character, scene, default twelve-photo count and optional brief as initial authoring inputs. Carry current selections forward; default automatic if configured, otherwise manual with Configure assistant. Keep explicit mode switch accessible.
- [ ] 6.2 Put raw take fields, variation locks and look/wardrobe overrides under Advanced while preserving first-class manual editing. Connect shared summary/suggestions to authoritative saves.
- [ ] 6.3 Replace sequential session/plan writes with atomic creation. Connect Prepare, Cancel, Resume and Continue preparation to backend progress/ownership and show assistant-unavailable or failed states with next actions.
- [ ] 6.4 Make effective choices primary in Review, with prompt/provenance details, duplicate flags and downstream-edit impact visible. Retain explicit approval, submission/run and small test-selection gates; preparation never triggers them.
- [ ] 6.5 Add interaction-level tests for the ordinary path, advanced/manual access, state recovery, stale responses and no implicit generation. Include a reproducible browser walkthrough, not only pure-helper tests.

## 7. Reusable personal looks and explicit wardrobe progression

This group depends on shared-state persistence and the field-preserving transport. Complete it before the final acceptance group.

- [ ] 7.1 Reuse existing garment/outfit catalogue semantics and add a minimal named saved-look wrapper separating appearance from ordered garments. Create/edit through forms with hidden technical keys; edits create immutable versions/new records and never reword used legacy keys.
- [ ] 7.2 Implement versioned portable JSON import/export and existing garment/outfit JSON compatibility. Do not delegate writes to /api/wardrobe/import: implement a full preflight, identity/content comparison and transactional revalidation plus atomic writes; identical re-import is a no-op, conflicting content needs explicit new-version/save-copy resolution. Enforce 10 MiB and omit private photos/paths/session data from exports.
- [ ] 7.3 Add one-image browser upload and explicit Extract look using the existing configured vision transport. Use already-installed Pillow for actual JPEG/PNG/WebP verification and complete decode, 10 MiB actual bytes and 25 megapixels before inference; enhance data-URI syntax checks alone are insufficient; stage privately with opaque IDs and 24-hour expiry/cleanup. No extraction on selection or automatic generation-reference attachment.
- [ ] 7.4 Return editable visible appearance and garment proposals, identify unknown details, and require user-confirmed removal order. Persist the specified request projection with exact text, non-secret model parameters and digest/media type/bytes/dimensions substituted for each binary part, plus output/corrections; never store raw data URIs or credentials; no save on failure and manual creation remains available without vision support.
- [ ] 7.5 Populate the already-defined authoring.look_snapshot with server-verified preset identity/version/digest, appearance and complete garment wording/order; do not introduce another plan shape. Require Replace/Keep for existing overrides and explicit CAS reapplication; later library edits cannot change sessions or prepared history.
- [ ] 7.6 Add Keep clothing default and an explicit pure snapshot-argument progression derivation using existing authored garment arc semantics; do not call live-global arcFor or filter missing garment identities. Let users choose order, final stage and take interval; apply the specified deterministic distribution only when sufficient takes exist, with explicit no-clothing wording for the final garment-free stage.
- [ ] 7.7 Apply reviewed progression as initial_wardrobe and minimal stable-ID from_here events in one CAS save, with explicit existing-event replacement/merge and preserved this_take behavior. Keep approved events authoritative after reordering rather than redistributing silently; preserve invalidation and history guards.
- [ ] 7.8 Show the personal Looks editor and optional session selector, keeping appearance separate from removable garments/accessories. Ensure final prompts contain only current garments, unchanged wording is carried verbatim, and initial full-outfit text is not appended to constant look or writer output.
- [ ] 7.9 Test manual/JSON/photo creation, malformed imports with zero writes, duplicate/conflicting keys, one-piece garments/layers, corrupt/truncated/oversized decoded images, unavailable vision, request-evidence redaction, user corrections and image cleanup. Verify snapshot derivation ignores live catalogue changes and refuses missing/duplicate garments; assert zero writes after a late invalid import item. Use invented text and temporary generated image fixtures; never track user photos.
- [ ] 7.10 Demonstrate a look reused in two sessions, editing its library version without changing either snapshot, and a reviewed full-to-partial-to-garment-free progression with fixed appearance. Verify too-short intervals, scope overrides, reorder, source-clothing conflict, repeatable prompts and no implicit generation.
- [ ] 7.11 Update README and matching documentation with personal looks, text/JSON/photo workflows, snapshot behavior and explicit clothing progression. Final repository gates run once in 8.6 after all implementation groups are complete.

## 8. Acceptance, compatibility and documentation

- [ ] 8.1 Demonstrate the complete automatic journey with invented source files initially unimported: select/import, resolve missing translations through reviewed proposals, choose character/room/count/brief, accept optional shared suggestions, prepare twelve takes, interrupt/resume, edit an early take, review and explicitly generate a test subset. Require no typed paths/keys, external JSON editing or per-photo four-field input.
- [ ] 8.2 Demonstrate no-map/no-assistant translation rows, manual shared/take choices, preparation and review; verify automatic drafts remain valid while synthesis is unavailable.
- [ ] 8.3 Demonstrate historical differently keyed import targeting, supported non-envelope import and a fused conflict requiring advanced review/adaptation. Verify immutable history and canonical import accounting.
- [ ] 8.4 Verify legacy creation, measured-catalogue gates, expert multi-resource plans, path automation, translation contracts, queued/generated snapshots and runner/reference behavior remain unchanged. No Delete/Restore schema or API is introduced.
- [ ] 8.5 Update README and matching resource/session docs with the simple journey, readiness actions, reviewed suggestions, fused limitations, batch limits, ownership/recovery behavior, retained expert controls and explicit generation steps.
- [ ] 8.6 Run full python -m pytest, npm --prefix frontend test, npm --prefix frontend run build, git diff --check, repository privacy/control-character checks and openspec validate simplify-resource-session-workflow --strict --no-interactive. Report actual commands, outcomes and blockers; mark tasks complete only from verified implementation.
