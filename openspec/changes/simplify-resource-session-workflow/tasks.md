## 1. Browser-selected import protocol and compatibility

- [ ] 1.1 Add the persisted selection/file/commit-claim model with the exact states, fixed 24-hour expiry, revision invalidation, atomic streaming reservations and server-only paths/fingerprints from design.md; implement startup and lazy recovery plus retryable cleanup warnings, and verify state transitions, aggregate races and cleanup without touching committed resources.
- [ ] 1.2 Implement the closed create/upload/remove/choice/status/cancel HTTP contract and safe SelectionView, including request/upload replay, stable error bodies, highest-revision handling and JavaScript-safe serialization; verify different bytes under one upload ID conflict and `mtime_ns > Number.MAX_SAFE_INTEGER` never leaves the server.
- [ ] 1.3 Adapt staging to canonical preview/commit fingerprints, attestation, duplicate accounting and atomic accepted-set persistence. Persist selection commit result in the same transaction as accepted resources, recover stale claims, return `202` to the same active commit tuple and replay committed results without re-import; verify two simultaneous commits and response-loss retry. Retain the path API/CLI unchanged.
- [ ] 1.4 Default valid declared library identity exactly; adapt collection-only `items` envelopes lacking library in the browser path without changing legacy parser defaults, preserving bytes and identical preview/commit interpretation. Resolve ambiguous auxiliary candidates through explicit manifest-bound Advanced choice, support explicit historical/new targets, and verify overlap, unknown `random_payload`, marker-bearing ambiguity and unchanged legacy classification.
- [ ] 1.5 Replace normal path/key entry with browser file selection backed only by SelectionView. Show readable preview, compatibility controls, terminal/cleanup warnings and current revision; ignore lower or superseded responses and never display paths/raw fingerprints. Keep Resource Browser filtering limited to safe library-list fields (key, source ID, digest and translated values), never an omitted raw payload.
- [ ] 1.6 Add backend and real frontend-contract tests for limits, cross-selection IDs, stale revisions/previews, expiry/tombstone/purge, cancel/commit conflict, same-library multi-file duplicates, crash recovery, safe response shapes and library filtering against actual payload-free list responses; use selected fake files and no private paths.

## 2. Shared request limits, assistant transport and preparation authority

- [ ] 2.1 Add one reusable actual-streamed-body limit boundary that can reject content-bearing JSON or upload requests before JSON/Pydantic parsing or writes even when Content-Length is missing or false; verify exact-boundary and oversized streams for each later consumer.
- [ ] 2.2 Add/reuse a field-preserving `backend.enhance` helper with existing provider URL, authentication, timeout and error sanitization while leaving historical flattened callers unchanged; verify structured fields/order, provider fallback behavior and no credential exposure.
- [ ] 2.3 Enforce the automatic/manual/pre-authoring authority matrix. Reject public begin/complete on every plan carrying authoring metadata and reject `manual_completion` for automatic authoring; restrict public raw completion to pre-authoring expert plans; retain validated manual completion only for manual authoring and preserve legacy composition. Verify every rejection occurs before ready-state mutation or assistant work.
- [ ] 2.4 Make authoring preparation derive final prompt, effective state, compiler/mapping versions, exact request/output, predecessor/resource projections, provenance, duplicate flags and digests server-side. Record manual inputs as manual provenance and assistant output only from a fenced operation; Approve/Recovery/Submit must reject missing, altered or fabricated evidence.
- [ ] 2.5 Test forged final prompts, effective state, assistant output/provenance/digests, automatic manual completion, locked/manual overrides, raw compatibility calls and pre-authoring expert completion. Update existing tests that currently treat arbitrary client provenance as authoritative rather than weakening their other compatibility assertions.

## 3. Actionable translation readiness

- [ ] 3.1 Use selected translation-map content through existing bulk preview/apply with the shared 10 MiB actual-body boundary and unchanged authorization/digest/library-fingerprint checks; verify missing/false Content-Length returns 413 before parsing/writes.
- [ ] 3.2 Build direct `translation_map` payloads from editable source-backed rows and route them through canonical bulk attestation, never the direct revision shortcut. Preserve scalar/list contracts, diagnose incompatible duplicate source strings and include explicit identity translations for already-English required strings.
- [ ] 3.3 Use the field-preserving transport for optional proposals of at most twenty source entries per action. Refuse unsolicited/malformed fields, preserve canonical keys/list order and make proposals write-free until reviewed preview and explicit apply.
- [ ] 3.4 Present imported, needs translation, ready and needs source correction with direct actions. Refresh readiness after apply and discard stale proposal/preview responses.
- [ ] 3.5 Verify no-map/no-assistant manual translation, invalid-source versus missing-translation diagnostics, assistant/provider failures, unauthorized proposals, conflicting duplicates, stale map/library state and bounded real response bodies.

## 4. Closed authoring contract, workflow binding and atomic guided creation

- [ ] 4.1 Implement the complete closed authoring-v1 schema, including server-owned `workflow_binding`, evidence and nullable look/progression blocks. Generic plan saves may only echo owned blocks; frontend/backend normalization preserves every member, empty take arrays and invalid/missing IDs without fabricating content. Verify forged/unknown blocks and malformed policy unions are rejected.
- [ ] 4.2 Preserve plans without authoring metadata and established expert/legacy count behavior. Validate integer guided/new-growing authoring counts 1-500 and brief <=2,000 before allocation; reject booleans, fractions and numeric strings without a twenty-take plan cap.
- [ ] 4.3 Resolve effective workflow server-side from explicit Advanced override then character default. Validate existence and existing resource workflow/reference compatibility; persist ID/kind/canonical graph and node-map digests, reject absence with actionable `workflow_required`, and reject changed/missing bindings at preparation/approval/submission. Verify later model-default changes have no effect and authoring workflow replacement requires a new session.
- [ ] 4.4 Implement `POST /api/sessions/guided` and the durable guided request record. Canonically bind request ID to normalized-body digest and persist claim/session/plan/stable IDs/stored response atomically; verify new `201`, replay `200`, changed-body `409`, response-loss retry, concurrent same-token creation of at most one session and rollback with no request/session/plan orphan.
- [ ] 4.5 Make plan look/initial wardrobe authoritative for all resource-v1 detail/list/search projections, leave guided legacy columns empty, reject legacy session PATCH for those fields and preserve legacy row/search behavior. Verify plan edits immediately change listing/search without mirror writes and missing resource plans are diagnosed.
- [ ] 4.6 Implement the assistant-free shared-state summary from authoritative plan values and authorized scene descriptions. Empty additional constraints do not erase scene descriptions; saved values and origins remain distinct.
- [ ] 4.7 Extend generated continuity freeze to scene anchor, variation policy, workflow binding and the entire look snapshot, including null/version/order/aside, without rewriting generated/queued evidence. Keep explicit scoped wardrobe changes authoritative.
- [ ] 4.8 Test schema round trips, workflow resolution/drift, source-of-truth projection, mode changes without assistant configuration, conflicting fixed values, stale CAS and plans above twenty takes against actual API response shapes.

## 5. Recoverable authoring operation foundation

- [ ] 5.1 Add the persisted authoring-operation model exactly as designed: request/operation identity, kind, plan revision, request digest, active/cancel-requested/terminal states, one non-terminal operation per session, monotonic fencing token, ten-minute lease, ordered progress/result/error and timestamps. Verify database constraints and terminal evidence retention.
- [ ] 5.2 Implement operation start/status with the closed OperationView and error contract. Enforce request replay, changed-body conflict, shared exclusion across suggestion/preparation kinds, target validation, assistant/configuration gates and active-operation response without launching remote work.
- [ ] 5.3 Implement backend-only lease renewal and fenced response persistence: renew before remote calls/scheduling, keep transactions outside network I/O, and atomically verify token/state/lease/revision/request inputs/resource/workflow validity before persisting result and progress. Verify late owners and stale revisions cannot write.
- [ ] 5.4 Implement cancel/resume plus startup/lazy recovery. Cancel stops scheduling and discards in-flight output; startup expires prior-process active owners and finalizes cancel requests; Resume increments fencing and reuses completed work only while inputs remain current. Verify feature-disable cancellation and readable cleanup/recovery warnings.
- [ ] 5.5 Implement succeeded shared-suggestion acceptance through the operation-specific idempotent plan CAS. Bind accepted/edited values to validated output, persist exact origins/evidence, replay a lost acceptance response and reject different acceptance content or stale revision.
- [ ] 5.6 Test simultaneous tabs and mixed operation kinds, start/accept response loss, request-ID misuse, cancellation during a call, lease expiry/new fence, process restart, plan edit race, assistant unavailable, partial failure/resume and operation status after terminal states using fake assistants and injected time.

## 6. Shared suggestions, safe scene handling and structured synthesis

- [ ] 6.1 Restrict simple automatic anchors to ready rooms while retaining fused/manual/expert paths. Show Advanced or structured-scene alternatives and never auto-decompose fused prose.
- [ ] 6.2 Show complete authorized fused descriptions and require a reviewed adaptation or another resource for conflicts. Make adaptation recording use `_prepare_resource` or the shared authorized resolver, persist exact effective `source_value`, and revalidate it in review/finalization; verify translation-versus-payload values, missing required translation and changed sidecars without rewriting history.
- [ ] 6.3 Run optional missing look/wardrobe suggestions only through the exclusive `shared_suggestions` operation after the operation foundation exists. Require idempotent reviewed acceptance before take consumption, preserve exact input/output/edits/origins and support explicit empty/manual decisions.
- [ ] 6.4 Bridge `prepare_takes` operation output into resource preparation validation/finalization with real assistant provenance. Request only unlocked camera/framing/pose/expression and prevent output from mutating resources, adaptations, workflow or shared state.
- [ ] 6.5 Test locked-field refusal, malformed/partial output, placeholders, shared-state protection and provenance replay. Demonstrate fused camera/pose conflict and location embedded in pose without advertising structural validation as semantic approval.

## 7. Ordered preparation, dependency integrity and copy-forward

- [ ] 7.1 Build deterministic context from persisted brief, anchor/authorized descriptions, shared values, workflow binding, policy, take ID/ordinal and at most five preceding finalized four-field summaries. Omit total count from requests/creative digests, persist exact context and predecessor identities/revisions, and verify stable ordering.
- [ ] 7.2 Implement sequential `prepare_takes` operation batches of at most twenty with per-take fenced persistence, partial failure state and Continue for larger plans. Reuse current ready/generated snapshots and verify forty-take continuation without regenerating the first batch.
- [ ] 7.3 Implement conservative automatic downstream invalidation from the earliest edit/removal/reorder plus global authoring and existing wardrobe rules. Revoke review/fence operations on mode/input changes while preserving generated history and manual input-based behavior; verify deleting take ten preserves eligible earlier work.
- [ ] 7.4 Add the versioned effective-resource-input projection/digest using the shared resolver and exact consumed adaptations. Revalidate it in read-only review/recovery and distinguish changed consumed values from unused sidecar metadata; verify missing legacy evidence is never backfilled.
- [ ] 7.5 Implement verified snapshot copy-forward inside plan CAS for eligible ready, unlinked, non-invalidated rows with byte-identical prompt, original synthesis evidence, destination binding and lineage/content digest. Exclude pending/linked/stale/unverifiable rows and verify unique destination, conflict rollback and old-revision submission refusal.
- [ ] 7.6 Revalidate and copy/reuse only consumed applicable adaptations in the same CAS, preserving exact source/adapted values and source/destination lineage with `adaptation_digest`. Reject conflicting destination rows atomically and require fresh review for missing/inapplicable approvals; update schema comments/docstrings to name this sole inheritance exception.
- [ ] 7.7 Integrate resource/workflow validation into authoritative Review, Recovery, Approve and domain Submit transactions, including selected batches. Serialize dependency checks with linking, make one stale batch member cause zero shot/link writes, keep approvals ineffective after drift and preserve already-linked history.
- [ ] 7.8 Implement idempotent `refresh-resources` CAS with current-revision/no-drift no-op, unchanged creative fields, operation fencing, affected-take diagnostics and verified unaffected copy-forward. It must perform no assistant, approval, submission or generation work; verify concurrent retry and fresh adaptation under the next revision.
- [ ] 7.9 Flag normalized exact four-field duplicates against one representative per take/copy lineage plus genuine generated history, excluding the candidate lineage and stale/invalidated unlinked rows. Preserve deliberate repeats and require review without deletion or unbounded retry.
- [ ] 7.10 Exercise copy-forward `recover -> approve -> submit`, translation/context/workflow mismatch, duplicate-copy conflict, adaptation visibility/conflict rollback, no duplicate-of-self, ready-approved translation drift, direct domain submission, translation/submit race, selected-batch zero writes, refresh/no-drift/retry and linked-history no-regeneration.

## 8. Guided creation, operation and review UI

- [ ] 8.1 Show character, ready scene, default twelve-photo count and optional brief initially. Carry selections forward, default automatic only for configured text synthesis, and show workflow-required remedies; keep explicit mode and workflow override under Advanced.
- [ ] 8.2 Put raw take fields, variation locks and look/wardrobe overrides under Advanced while preserving first-class manual editing. Connect shared summary and suggestion acceptance to authoritative CAS operations.
- [ ] 8.3 Replace sequential session/plan writes with idempotent guided creation. Reuse the same request ID while outcome is unknown, handle `201/200/409` distinctly and navigate only from the stored response.
- [ ] 8.4 Connect Prepare/Continue/Cancel/Resume to OperationView and adopt active work from another tab. Show completed/failed/remaining, assistant-unavailable, stale, cancelled, expired and cleanup-warning states with exact next actions; never renew leases from polling.
- [ ] 8.5 Make plan-owned look/wardrobe primary in resource session detail/cards/search and make effective choices primary in Review. Expose prompt/provenance/duplicate/drift/downstream impact details while retaining explicit Approve, Submit/Run and small test-selection gates.
- [ ] 8.6 Add component/integration tests for Resources and SessionView against real backend response fixtures, including safe selection revisions, `prepared`/progress shapes, two-tab ownership, guided retry, workflow errors, plan/session projection, stale responses and no implicit approval/submission/generation. Include a reproducible browser walkthrough, not only helper tests.

## 9. Reusable personal looks and explicit wardrobe progression

This group depends on the shared body limiter, field-preserving transport, closed authoring schema and operation foundation. Complete it before final acceptance.

- [ ] 9.1 Reuse garment/outfit catalogue reads and add the minimal versioned saved-look wrapper separating appearance from ordered garments. Forms hide technical keys; edits create immutable versions/new records and never reword used legacy keys. Verify one-piece garments, layers and accessories.
- [ ] 9.2 Implement closed `portable-look-v1` parsing, canonical ordering/digest, allowed untrusted provenance and canonical export equality. Validate the entire envelope before writes and verify malformed/unknown content, private photos/paths/session data and credentials never export.
- [ ] 9.3 Implement transactional portable import preflight/revalidation, exact new-version/save-copy conflict choices and atomic writes without delegating to `/api/wardrobe/import`. Persist/deduplicate pre-remap content receipts with original/destination identities and exact local mappings; verify identical re-import, remapped retry, concurrent conflict and zero writes after a late invalid item.
- [ ] 9.4 Add legacy garment/outfit JSON as outfit-only input through the same preflight and shared 10 MiB actual-body boundary while leaving `/api/wardrobe/import` semantics unchanged. Verify missing/false Content-Length, unknown garment references and explicit conversion to a named look.
- [ ] 9.5 Implement one-image opaque staging with actual JPEG/PNG/WebP Pillow verification/complete decode, 10 MiB file and 25-megapixel limits. Add startup/lazy expiry, save/cancel cleanup and retryable warnings; verify corrupt/truncated/polyglot/oversized inputs and no inference on selection.
- [ ] 9.6 Enforce the visual capability predicate: configured text endpoint/model plus explicit non-empty verified/declarative `llm_vision_model`; store a detected visual text model explicitly and never fall back to a text-only model. Verify `409 vision_unavailable` before transmission, visible remediation and `502` provider image refusal with no saved look.
- [ ] 9.7 Produce editable appearance/visible-garment proposals with unresolved details and user-confirmed removal order. Persist redacted request projection, model/non-secret parameters, binary digest/type/bytes/dimensions, output and corrections; verify no raw data URI, image bytes, endpoint or secret headers persist.
- [ ] 9.8 Apply a saved look through explicit CAS using server-loaded preset identity/version/digest, appearance and complete garment wording/order in `authoring.look_snapshot`. Require Replace/Keep, preserve later catalogue independence and enforce continuity freeze.
- [ ] 9.9 Implement Keep clothing default and pure snapshot-argument progression using established arc semantics. Refuse missing/duplicate garments, implement canonical zero/one/many sentences identically backend/frontend, require sufficient interval and verify moved-aside/final garment-free stages byte-for-byte.
- [ ] 9.10 Apply a reviewed progression preview as initial wardrobe plus minimal stable-ID `from_here` events in one CAS with explicit merge/replace and preserved `this_take`; keep approved events authoritative after reorder without implicit redistribution.
- [ ] 9.11 Build the Looks editor, JSON/photo actions, optional shared-summary selector and progression timeline while keeping appearance separate from removable clothing and photo extraction separate from generation references.
- [ ] 9.12 Test manual/JSON/photo creation, unavailable/text-only vision, provider failure, evidence redaction, cleanup lifecycle, concurrent imports, user corrections, snapshot independence from live catalogue, malformed snapshot refusal and final prompts containing only current garments. Use invented text and temporary generated images only.
- [ ] 9.13 Demonstrate one look reused in two sessions, immutable library edits, full-to-partial-to-garment-free reviewed progression, too-short interval, scope overrides, reorder, source-clothing conflict, repeatable prompts, full snapshot freeze and no implicit generation.

## 10. Feature-flag rollback, compatibility, documentation and final acceptance

- [ ] 10.1 Extend the feature-flag matrix to every new selection/translation/guided/authoring/adaptation/refresh/approval/look/photo/preset/progression write. Preserve status/read/cancel and already-approved Submit/runner behavior, discard in-flight output on disable, and verify each endpoint/direct caller returns 503 or remains available exactly as specified.
- [ ] 10.2 Implement and document the mandatory verified-backup -> disable -> cancel/observe terminal -> verify -> clean stop downgrade procedure. Test rollback-compatible binaries cannot write away authoring metadata while disabled and state that binaries predating the flag require restoring the verified pre-upgrade backup.
- [ ] 10.3 Demonstrate the complete automatic journey from unimported invented files through safe selection/import, reviewed translation, idempotent guided creation, optional shared suggestion, twelve-take preparation, interruption/resume, early edit, review and explicit test-subset generation without typed paths/keys or per-photo four-field entry.
- [ ] 10.4 Demonstrate no-map/no-assistant manual translation, shared choices, manual completion and review; verify automatic drafts remain valid but synthesis unavailable and a text-only assistant cannot receive look images.
- [ ] 10.5 Demonstrate historical differently keyed targeting, non-envelope import, fused advanced adaptation, immutable accounting, committed-result replay and staging cleanup without adding Delete/Restore.
- [ ] 10.6 Verify legacy creation, measured-catalogue gates, expert multi-resource/raw completion, path automation, translation contracts, session listing/search, queued/generated snapshots, operational rollback and runner/reference behavior remain unchanged.
- [ ] 10.7 Update README and matching resource/session documentation once with selection/status contracts, readiness, guided workflow resolution/idempotency, authority matrix, operation lifecycle, plan-owned constants, Looks/photo/progression, feature flag/downgrade, retained expert controls and explicit generation gates.
- [ ] 10.8 Run full `python -m pytest`, `npm --prefix frontend test`, `npm --prefix frontend run build`, `git diff --check`, repository privacy/control-character checks and `openspec validate simplify-resource-session-workflow --strict --no-interactive`. Report actual commands/outcomes/blockers and mark tasks complete only from verified implementation.
